# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules

# vite build output
/dist
.vite
*.local

# testing
/coverage

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# typescript
*.tsbuildinfo

# Local orchestration scratch (git worktrees, subagent progress ledger)
/.worktrees/
/.superpowers/

# Local AI tooling config — not tracked upstream (this repo is public)
/.claude/
# Local MCP server config (not shared upstream)
/.mcp.json
