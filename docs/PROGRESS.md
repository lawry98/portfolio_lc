# PROGRESS — running log

> **Start every session here.** Read `docs/*` first — [`SPEC.md`](SPEC.md) (authoritative), [`TICKETS.md`](TICKETS.md) (plan), [`DECISIONS.md`](DECISIONS.md), [`ASSET_SPEC.md`](ASSET_SPEC.md), [`AUDIO_SPEC.md`](AUDIO_SPEC.md) — then only the files the current ticket touches. Never re-read the whole repo.

## Workspace
- **Branch / worktree:** `feat/byte-pet-demo` at `/Volumes/SD500/Documents/personal/portfolio_lc/.claude/worktrees/byte-pet-demo`
- The Vite + TS app root = **worktree root**. Keep the **SD500 drive mounted** (a disconnect deletes the worktree from view until remounted).

## Execution model
- **Style:** `superpowers:subagent-driven-development` (chosen 2026-08-21).
- Per ticket: expand into bite-sized TDD steps → fresh subagent per step-group → review between → commit per ticket → **`/clear` between tickets**. T6 is a hard **owner play-test** stop.

## Log (newest first)
- **2026-09-24 — Byte rests beside the caret (D-24) — built, awaiting the owner's go-ahead to commit.** Owner request: stop covering the last letter. Rendered real-model options in headless Chrome (a URL-gated mock, removed); a lean-on-the-caret version was built and tried live, and the owner kept the position but asked for Byte upright. New pure `src/pet/restPose.ts` + `restPose.test.ts`; `createBytePet.ts`: `restFeetX` at every home write, rightward-only slide, proximity from Byte's centre, per-tick active-caret visibility re-assert. QA in headless Chrome (CDP scratch driver): 1440 idle + curious, 768 and 360 idle, footer after a wheel-scroll migration (caret visible). Byte's box starts 12–24px right of the text end, right edge ≥ 31px inside the section at 360px. Pre-existing footer-caret bug found and fixed: proven with a MutationObserver on the pre-change code (caret shown at 4.8s by the migration, replaced by a hidden clone at 6.6s by the footer reveal). `npm test` 370/370, `tsc --noEmit`, lint, prettier clean.
- **2026-09-24 — Fix: head shell speckling through Byte's visor — PASS (D-23).** Owner report: light patches of the grey head shell showing through the dark visor in the light theme, bigger at some head angles. Root cause: depth precision. The camera used near 0.1 / far 100000 at ≈ 1679 units back, so a 24-bit depth step at z=0 was ≈ 1.7px while the visor sits ≈ 0.7px in front of the shell (the residual R-GLB-19 accepted). Reproduced in headless Chrome with a magnified canvas crop (ragged visor bottom rim, seams across the eyes); a one-variable experiment (near = 100) cleaned both, confirming the cause. Fix: exported `cameraClipPlanes(distance)` in `scene.ts` (near = distance / 10, far = distance × 10), applied in `applyViewport()` on every resize; the constructor no longer passes planes. Two new `scene.test.ts` tests (failing first): the depth step at z=0 stays < 0.007px for 600–2160px tall viewports, and > 500px of room remains in front of and behind z=0. Post-fix crops at 1440×900, light + dark, DPR 2 + DPR 1: straight visor rim, clean eye ovals. `npm test` 368/368, `tsc --noEmit`, lint and prettier clean. QA row 17 updated.
- **2026-09-24 — Fix: every migration after the first froze in `traveling` — PASS.** Symptom (1440×900, reproduced on `ff20f5c`/`a818fd2` and on the T-GLB branch, so it predates T-GLB): hero→footer migrates fine, but scrolling back up enters `traveling` and never leaves it, with nothing drawn in the hero and no console errors. **Root cause:** `runMigrationFull`'s arrival branch called `gsap.killTweensOf(rig.object3d.position)` to hand the root to the reacquire glide. In gsap 3.15 that clears the lane quickTo tweens' PropTween lists (`_pt = 0`) but leaves them initialised, so each later `byteToLaneX/Y(...)` → `resetTo` re-attaches the tween to the root timeline and updates orphaned PropTweens, and nothing writes the root again. The return leg's Byte therefore never moved (frozen at one position for 8 s+), `dist` never dropped under `MIGRATE_ARRIVE_DIST_PX`, `ARRIVED` never fired, and the stage stayed the off-screen footer's (R12-3 clear-and-skip), which is why nothing drew. The T8 QA note below ("proven functional out-of-band") was wrong. **Fix:** new `haltQuickTo(...setters)` in `motion.ts` pauses the setters' own tweens (the next call resumes them, the way a fresh quickTo starts paused), and the arrival branch uses it on the lane pair. The D-16 single-writer hand-off is unchanged. `destroy()` still kills outright. A/B showed the retype-follow `byteToCaretX/Y` pair was *not* affected: its tweens have always completed and detached from the timeline before an arrival, so the kill never reached them. **Regression tests** (`motion.test.ts`, +3, driven by `gsap.updateRoot`): halting stops an in-flight ease; a halted setter drives a second leg (fails if `haltQuickTo` is mutated to kill); a characterization test pins gsap's kill-disables-quickTo behaviour. **QA:** headless Chrome over CDP (Node 22 `WebSocket`, ~62 fps rAF) with a temporary uncommitted `window.__byteQA` hook, reverted before commit. Before the fix: `idle→traveling` at the return leg with the position frozen. After: four instant-scroll legs hero→footer→hero→footer→hero each arrive in 0.2–0.7 s; a Lenis mouse-wheel round trip (40 × ±400 deltaY, 40 ms apart) arrives both ways; Byte is drawn back in the hero; a post-migration feed runs `dashing→eating→retyping→idle` with Byte following the caret. `npm test` **299 passed**, lint/format:check/build clean, JS 209.66 KB gz.
- **2026-09-23 — T-GLB DONE.** Byte GLB swap-in — the real `byte.glb` replaces the procedural placeholder everywhere it appears (SPEC §4.2/§4.4/§6/§13/§14). Ticket commits `a818fd2..1f91c20` (9: `3f82e1b` rig contract extension, `3b65ab6` GLB rig `glbRig.ts`, `22f7b7c` swappable rig `swapRig.ts`, `c61e7e3` drive Byte through the swappable rig, `3710595` meshopt loader + `glbAsset.test.ts` smoke test (commits `public/models/byte.glb` unchanged, sha256 `f09c5b98…`), `1382603` load behind the preloader gate, `b188a43` retime the choreography, `ff7f480` size Byte at 1.25×, `1f91c20` the Task 7b QA fix) + this docs commit. New modules `src/pet/glbRig.ts` (crossfaded mixer clips, post-mixer look, eye blink, materials) and `src/pet/swapRig.ts` (stable root/pose, state replay, the swap pop); new test files `glbRig.test.ts`, `swapRig.test.ts`, `glbAsset.test.ts`, `src/page/preloader.test.ts`. `PetOptions.modelUrl?: string` + `BytePetHandle.modelReady: Promise<void>` (settles once the GLB is live or has failed; never rejects); the loader arrives through a dynamic `import('./glbLoader')` in its own lazy chunk, `MeshoptDecoder` bundled with three, the never-shipped DRACO wiring removed. The swap: instant while `hidden`, otherwise at the next resting state with a ~0.3s pop (squash out / `back.out` spring in), instant under reduced motion. Choreography retimed to the delivered clips — feed bites at 0.33s/0.60s, FSM wake window 1.25s via `createFSM({ wakeMs })` (`fsm.ts` untouched), peek 1.667s with behind/front swaps at 0.2s/1.45s. Byte's size is now 1.25× the headline font-size, one constant multiplying `unitPx` at its source. Fixed a **latent T9 bug** along the way (R-GLB-2): three r185 never recompiles a material's `#define OPAQUE` when only `transparent` flips, so the reduced-motion peek fade never actually faded before this ticket — routed through `rig.setOpacity` instead. Tests **296 → 363** (18 → 22 files; 353 at the Task 8 docs commit above, +10 from the final-review fix wave, D-22); lint, format:check, build clean. Budgets (measured, superseding the DoD's estimated "entry ≈ today"/"loader ≈ 33 KB", R-GLB-5): entry JS **225.28 KB gz** (was 209.65; +15.6 KB because `GLTFLoader`/`AnimationMixer` need three's core classes, which live in `three.core.js` and can't be split from the entry), lazy `glbLoader` chunk **21.00 KB gz**, JS total **≈246.3 KB gz ≤ 280** ✅; CSS 3.34KB gz unchanged; `build.chunkSizeWarningLimit` 750→850 (raw entry 798KB, gzip budget governs). Model `byte.glb`: **728,928 B raw, 526,881 B gzipped (≈525 KB gz)**, 36,283 tris, no textures — the ~5% over the soft 500KB target is the accepted exception (row 2, D-22). **No new runtime deps** (`GLTFLoader`/`MeshoptDecoder` ship in `three`). Via subagent-driven-development: 7 implementation tasks (6 code + 1 QA fix) + per-task Sonnet implementers/reviewers + browser QA (next entry) + this docs task. DECISIONS **D-22**; rulings **R-GLB-1..21** (R-GLB-19 the eye/visor depth-bias QA fix; R-GLB-20/21 the headless-Chrome QA method). Full audit trail: `.superpowers/sdd/t-glb-plan/`. **T-GLB→DONE; branch stays where the owner left it — merging/PR is the owner's call; NEXT→T-Audio (when audio files are delivered) or the owner's PR.**
- **2026-09-23 — T-GLB QA (controller pass, headless Chrome — not the Browser pane).** The session's Browser pane pauses rAF while hidden, so this ticket's QA ran in real **headless Chrome** (`--headless=new`, driven over CDP by a scratch, dependency-free script) at HEAD `ff7f480`/`1f91c20`, against Vite dev `:5180`, reading state through a temporary uncommitted `window.__byteQA` hook (the T9 precedent, reverted before every commit — tree verified clean). Headless Chrome ran real rAF at 60–64fps and could emulate `prefers-reduced-motion`, so reduced motion was checked in-session for the first time. **Entrance (6/6 viewport×theme** — 1440×900/768×1024/390×844 × light/dark): `modelReady` 185–411ms, preloader lift ≈1150–1310ms, the GLB already in place at `entering`; `unitPx` = 1.25×font (130/88/64.4px for 104/70.4/51.5px fonts); no horizontal overflow; console clean. **Slow model** (CDP `Fetch` held the request 7s, R-GLB-21 — no code delay hook, no renaming `byte.glb`): preloader lifted at its 4s cap with the placeholder; the model resolved at ≈7.3s and swapped in at `idle` with the pop (`pose.scale.y` 1→0.47→1.12→1.00); console clean. **Forced 404** (via CDP `Fetch`, not a file rename): placeholder stayed for the session; `modelReady` resolved; console showed exactly one `[byte] byte.glb failed to load; keeping the placeholder.` warning plus the browser's own 404 line; no exceptions. **Feed×3:** `eatA`/`eatB` fired on the clip's bites (+333/334ms and +600ms into every eat, R-GLB-11); glyph halfway+shrunk at bite 1, swallowed+burst at bite 2; three retypes; counter `FED 0 → FED 3 GLYPHS`; also fed×3 at 390×844 light (touch-emulated). **Peek:** first peek 6.6s/7.6s after idle; duration 1.70s/1.66s; behind/front swaps at 0.2s/1.45s. **Curious/invited/look:** head yaw ±0.175 rad (0.5×clamp), eye slide ±0.056x/+0.036y (0.8×0.07/0.045) — row 4 exactly. **Blink:** eyes squash to 0.06 every ≈2.68s. **Sleep→wake→dash:** asleep ~30s idle; click→`waking`→1.25s later (1248ms wall)→`dashing` — the click counts as a feed. **Hero↔footer migration + footer retype:** migrates into the footer stage, retypes LET'S BUILD→SAY HELLO, FED increments, nothing drawn while scrolled to #manifesto. **Theme toggle mid-dash:** dark→light 120ms into `dashing`; GLB body took the light palette mid-dash; flow completed; Glow emissive 0 in light. **Shadow on Hop (R-GLB-18 watch):** hover peaks 0.0945u (≈12.3px) at 0.50s; shadow opacity 0.35→0.283 (−19%), scale 1→1.12 — the predicted ~20% reaction; surfaced to the owner, `shadow.ts` left unchanged. **Perf:** 10s idle trace 60.1fps, 0 frames >33ms, max 16.8ms. **Heap after 50 feeds:** 11.53→12.33MB after GC, DOM nodes 352→355, listeners 81→81, scene objects 35→35, front-layer children 1→1. **Reduced motion (emulated):** swap instant (no pop); the reduced peek now visibly fades (Body opacity 1→0.45 with `transparent` on, back to 1/off — the R-GLB-2 fix); rise ≈10px; blink continues; doubleSided inner faces faintly visible through the fade. **Eyes after R-GLB-19** (the eye-depth-bias QA fix, `1f91c20`): clean, stable mint ovals in light and dark; a faint speckle on the visor's bottom rim (same precision cause) remains, recorded as an on-device row. **Known issues surfaced, NOT T-GLB** (reproduced identically on the pre-T-GLB tree `a818fd2`; filed as follow-ups, not fixed here): a footer→hero scroll-back can leave the FSM stuck in `traveling` drawing nothing in the hero; a ~5.7px sliver of Byte draws above the footer box when scrolled to #selected-work with the footer partly in view (T12 stage clip); the page's first gesture (sound-gate unlock) costs one ~130–150ms long frame; at 390 touch-emulated the "(click to enable sound)" cursor label is visible before the first gesture; the dark-theme D-11 body palette is low-contrast (settled row 10, the owner already saw it in the design demo). On-device rows this headless pass couldn't settle (real OS reduced-motion, true 60fps on a real phone, the doubleSided fade and the visor speckle/eyes on real hardware, the slow-network pop on a real throttled connection) are recorded in `docs/qa/README.md` §4. Full evidence: `.superpowers/sdd/t-glb-plan/qa-headless-2026-09-23.md`.
- **2026-09-01 — T12 DONE.** Scroll bounds — stage clip, containment & hand-off fade; **supersedes SPEC §6's right-margin travel lane**. Branch `lc/byte-pet-scroll-bounds-25b3c2` off `4288559` (the T10 close); ticket commits `4288559..b9d7506` (5) + this docs commit. Byte and everything the pet module draws are now confined to two bounded **stages**, so nothing paints over `#manifesto` or `#selected-work` at any scroll position. New **pure** `pet/stage.ts` (136 lines, **zero imports** — grep-verified in review, like `anchor.ts`/`fsm.ts`): `stageFromSection(section, furniture)` (the section box, bottom-inset to the topmost furniture rect — **one rule generates both stages**), `intersectViewport(stage, viewport)` (`null` = the render-skip signal), `clampToStage(point, size, stage, pad)` (the point is the box **centre**) + **`stage.test.ts` (24 tests)**. `scene.ts`: `SceneHandle.setStage(rect | null)` + `setOpacity(a)` — per frame both renderers take a **full clear with the scissor test OFF**, then the scissor box, then render; `null` clears both and skips the draws (**R12-3** — skipping `render()` alone freezes the last frame on the compositor, the exact bug T12 fixes). `setStage` is **tri-state** (`undefined` = unconfigured → unclipped, so `createScene` stays non-breaking — R12-4). The scissor takes **CSS px, NOT device px** (**R12-1**: three@0.185.1 multiplies by pixel ratio internally at `three.cjs:76805`; device px would clip to a quarter-size region on retina), Y-flipped `glY = viewportHeight - (stage.y + stage.height)`. Two review fix rounds extracted that arithmetic as exported pure helpers beside `worldFromScreen`: **`scissorFromStage`** (+5 tests) and **`clampFeetToStage`** (+6 tests). `createBytePet.ts`: measures both sections + their furniture in `onTick`'s **existing** read batch (no second scroll listener), feeds the active stage (following `activeHome`) to `scene.setStage`, clamps Byte's root into it (`STAGE_CLAMP_PAD_PX` = 12), and tweens opacity **1→0→1** across the hero↔footer hand-off over `STAGE_FADE_DURATION_S` (0.3s) via `fsm.onEnter` (**subscription only**, R12-5 — the canvases carry only pet output, and no rig/shadow opacity API exists). `index.html`: three `data-byte-furniture` tags (`.hero__scroll`, `.footer__meta`, `.footer__copyright`) — furniture is **declared in markup** so `pet/` never names page CSS classes (**R12-6**). The footer section lookup is **`closest('section, footer')`** (**R12-9** — `<footer id="footer">` is a `<footer>` tag and a **sibling of `<main>`**, so the bare `closest('section')` returns `null` and the footer stage would silently never exist; `feedZone`'s bare selector at `createBytePet.ts:368-369` is deliberately left alone — it only ever sees the hero headline; _merge note 2026-09-22: D-20 has since replaced it with `feedZoneFor` in `feedZone.ts`, which follows the active home_). **`fsm.ts` and all T8 migration logic are UNTOUCHED** (verified across the whole branch; `fsm.test.ts`, 1103 lines, passes unchanged) — the lane still runs, it is simply clipped away for its whole traverse. **The fade is not the guarantee; the scissor is:** the implementer's "safe because opacity is ~0 throughout traveling" rationale was **ruled FALSE** in review (the fade-out ramps from 1 over 0.3s, and `fsm.ts:388`'s `traveling --FEED--> dashing` restores opacity to 1 for seconds mid-trip) and was corrected in the report **and** the code comments (`createBytePet.ts:1633-1643`, `scene.ts:121-130`) so no future reader removes the clip believing the fade protects them. **Owner-visible behaviour change:** the containment clamp is a **no-op at 1440×900** at both homes and the **footer never clamps at any viewport**, but on narrower viewports it pulls the **hero** anchor left — **−23px at 768×1024, −14px at 390×844** — because the bound is the **section** box (scrollbar-excluded), not the viewport. That is required by the chosen edge behaviour (design demo option **C**, "the clip stays as a backstop that never fires"): unclamped, Byte's right edge overflows the section by **11px at 768** and the scissor would visibly **slice** it. So on ≤768px Byte now rests slightly closer to the headline text than pre-T12 — fully on-stage and un-sliced, which it was not before. **`STAGE_CLAMP_PAD_PX` (12) is the single tunable** if the owner wants it further out. **No new deps. No `src/` file outside `pet/` changed** (the only page edit is the three furniture attributes). **928 insertions across 7 files**: created `src/pet/stage.ts` + `stage.test.ts`; modified `scene.ts`, `scene.test.ts`, `createBytePet.ts`, `types.ts`, `index.html`. Budgets JS **209.55KB gz** (T10 206.40; **+3.15**) / CSS **3.33KB gz** / fonts **26.15KB** unchanged (≪ 280/20). `npm test` (**285/285**, +35 vs T10's 250: 24 `stage.test.ts`, 5 `scissorFromStage`, 6 `clampFeetToStage`), `npm run lint`, `npm run format:check`, `npm run build` all clean. Via **subagent-driven-development**: 3 implementation tasks + per-task reviews (**Task 1** clean — spec ✅, quality Approved, 0 Critical/Important; **Task 2** Approved-with-1-Important → 1 fix round extracting `scissorFromStage` → scoped re-review ADDRESSED; **Task 3** Approved-with-1-Important → 1 fix round extracting `clampFeetToStage` + the rationale correction → scoped re-review ADDRESSED, with the no-op round-trip test confirmed non-vacuous) + **controller geometry/scissor QA (next entry)** + this docs task. **Three reviewer ⚠️s were closed with measurement, not argument:** the uncommitted `TICKETS.md` + the untracked demo files provably predated the branch; the retina/ghosting claim was settled by a live DPR-2 GL proof; the footer anchor's clearance was settled by measuring the real anchors. The 5 untracked `byte-bounds-*.html` design demos were deleted per the ticket DoD (backed up to the session scratchpad first, since untracked deletion is unrecoverable). DECISIONS **D-21**; rulings **R12-1..R12-9**. Deferred minors carried, none blocking: `types.ts:48-53` — `renderFront`/`renderBack`/`render` doc comments don't yet say they can clip or no-op; `types.ts:10,21` — the `import type` + `export type … from` pair is redundant *as duplication only* (the import is genuinely required; `export … from` creates no local binding); `furnitureRectsOf` (`createBytePet.ts:355`) re-runs `querySelectorAll` every tick over a static element set (~6-8 throwaway allocs/frame) while the sections themselves are cached; `clampFeetToStage` computes unconditionally (`createBytePet.ts:2063`) though the result is discarded outside the `home` branch; Task 1's report-count slip is **CLOSED** (report accuracy only, no code effect). T8/T9 minors + the **P-2 halo sprite** stay optional polish. Full audit trail: `.superpowers/sdd/t11-plan/`. **T12→DONE; branch stays OPEN — merging is the owner's call.**
- **2026-09-01 — T12 QA (controller geometry + scissor pass) — PASS on geometry; the animated checks are owner/on-device.** Method: vite dev on **:4599**, Browser pane, `resize_window` per viewport, real `getBoundingClientRect()` values — layout is real in this pane even though rAF is frozen. **The sweep runs the REAL shipped code, not a re-implementation:** the dev server serves `src/pet/stage.ts` as an ES module, so it is `await import('/src/pet/stage.ts')` driving the actual `stageFromSection`/`intersectViewport`/`clampToStage` against live rects. Wiring confirmed first — `[data-byte-furniture]` resolves to exactly **3** elements (`hero__scroll`, `footer__meta`, `footer__copyright`; 1 inside `#hero`, 2 inside `#footer`). **Static geometry at `scrollY = 0`** (CSS px, document coords): **1440×900** — hero stage 85→805 (h **720**), `#manifesto` starts 985 → **180px clear**; footer stage 2824→3231 (h **407**), `#selected-work` ends 2824 → **abuts exactly, no overlap**. **768×1024** — hero stage 85→904 (h **819**) → **205px clear**; footer stage 2384→2660 (h **276**) → **1px clear**. **390×844** — hero stage 99→774 (h **675**) → **169px clear**; footer stage 2228→2457 (**h 229**, w 390) → **abuts exactly**, matching the ticket's predicted "229×390 footer stage — tight but workable"; Byte's `unitPx` there is **52px**, so even the tightest real stage has ~4.4× Byte's own height of room and `clampToStage`'s inverted-range collapse is a genuine safety net rather than the normal path. `overlapManifesto: false` / `overlapWork: false` at every viewport — checked programmatically, not by eye. **End-to-end scroll sweep: 78 scroll positions across three viewports, ZERO overlap violations** — 1440×900 (20 samples, 100px step, **0**), 768×1024 (28 @ 50px, **0**), 390×844 (30 @ 50px, **0**); at each step both stages are built, clipped to the viewport, and asserted not to overlap `#manifesto` or `#selected-work`. **The render-skip path fires for real** (design row 8): positions where BOTH stages were `null` and both renderers skipped = **4 / 1 / 7** respectively — mid-page, neither stage intersects the viewport. **`clampToStage` on the tightest real stage:** `clampToStage({x:5,y:5}, {width:52,height:52}, {x:0,y:2228,width:390,height:229}, 12)` → **`{x:38, y:2266}`**, exactly the pad-plus-half-Byte boundary (`0+12+26` / `2228+12+26`). **Empirical WebGL scissor proof (real GL context, DPR 2):** a standalone WebGL2 canvas at 1440×900 CSS, scissored to the measured hero stage with scene.ts's own formula (`glY = 900 − (85+720) = 95` CSS px → 190 device px), sampled DOM y **80 OUTSIDE / 90 INSIDE / 800 INSIDE / 810 OUTSIDE** — **the Y-flip lands the band's edges on 85 and 805 to the pixel**. **R12-2 CONFIRMED empirically:** after re-scissoring to a different box and clearing, the previous stage's pixels were **still present** — `clear()` IS scissor-bound, so the ghost the scissor-off full clear prevents is a real bug, not a theoretical one. **What the clamp does to Byte's RESTING position** (real shipped `clampToStage`, real home anchors, measured with the same Range-based text measure the code itself uses at `createBytePet.ts:281-285` — the element rect is wider than the text and gives a misleading answer): 1440×900 `unitPx` 104 → hero Δx **0**, footer Δx **0**, Δy **0**; 768×1024 `unitPx` 70 → hero **−23**, footer **0**, Δy **0**; 390×844 `unitPx` 52 → hero **−14**, footer **0**, Δy **0**. Desktop is a complete no-op (Byte's rest is byte-identical to pre-T12); the footer never clamps at any viewport — which resolved the Task 3 reviewer's ⚠️ that had estimated the footer's clearance from CSS instead of measuring it. **Theme invariance:** flipping `<html data-theme>` light↔dark at 390×844 with a forced reflow between snapshots produced **byte-identical** rects for all seven boxes (`identical: true`) — the theme changes colour tokens only, never layout, so the computed stage and therefore the scissor box are theme-independent **by construction**. The both-themes DoD row is satisfied for the **clip** by that invariance; Byte's *appearance* per theme is unchanged by T12 (no material or light code was touched). **Limitation — stated plainly.** Both browser surfaces were checked, not assumed: `list_connected_browsers` returns `[]` (no real Chrome is connected to this account — the same unavailability T10 recorded), so the in-app Browser pane was the only surface, and there `document.visibilityState` is permanently `'hidden'` → **rAF never fires, `gsap.ticker` never runs, `onTick` never executes, and Byte never draws**. **Nothing animated was seen on screen, and no screenshot of Byte in motion exists for this ticket.** The hand-off fade in motion, a **mid-trip feed** (`traveling → dashing → traveling`, where opacity legitimately returns to full while the clip is still bound to the old stage — the exact case where the scissor, not the fade, is what holds), the occlusion weave, the FED counter incrementing, and reduced-motion on a real device are therefore **owner/on-device residuals**, exactly as T10 recorded in `docs/qa/README.md` §4. Full evidence + tables: `.superpowers/sdd/t11-plan/qa-geometry.md`.
- **2026-08-27 — T10 DONE.** Perf & release — the demo is release-ready (SPEC §13/§7/§16). Ticket commits `1a2028c..c7995dd` + this docs commit. **One type system in prod:** a build-only `lockFontsPlugin` (`vite.plugins.ts`: PURE `stripUnlockedFontFaces` + a `?used`-tolerant `/styles/tokens.css` id-match; 7 unit tests) strips the non-locked `@font-face` blocks so the release emits **only Space Grotesk** (500+700, ~26.15KB); dev still serves all 3 for `?lab`. `?lab` is `import.meta.env.DEV`-gated → tree-shaken from prod (boot glow/phrases stay OUTSIDE the gate, so mint+identity still ship). `vite.config.ts` pins ports + accepts the raw-chunk size (**R10-3**) + emits source maps. Docs: `README.md` (run/build/preview, budgets, lab, graceful degradation, integration pointer), `docs/INTEGRATION.md` (mount `createBytePet` in Next.js — client component, dynamic import, `destroy()` cleanup, framer-motion coexistence; real `PetOptions`/`BytePetHandle` API), `docs/qa/README.md` (this ticket's QA record). **a11y/BP fix wave (Task 4):** invalid SplitText `aria-label` on `.manifesto__line` spans → `aria:'none'`; muted-label contrast → `--muted` to AA (`#65636f` light / `#838093` dark, ≥4.5:1 on paper+paper-2 both themes); `/favicon.ico` 404 → `public/favicon.svg` + `<link rel=icon>`; no source maps → `build.sourcemap` (plugin returns an empty map — closes T1-m3). **No `src/pet/*` runtime change** (Byte byte-identical to T9). **No new deps.** Budgets JS **206.4KB gz** / CSS **3.34KB gz** / fonts **26.15KB** (≪ 280/20/120). Build/lint/format/`vitest` (**250/250**) green. Via `subagent-driven-development`: Tasks 1–3 + per-task Sonnet reviews (T1 +1 fix) + **Opus whole-branch review** (Ready=YES) + Task 4 (Sonnet impl + review + 2 fix rounds) + scoped re-reviews + **controller prod-packaging QA + Lighthouse re-run**. DECISIONS **D-18**; rulings **R10-1..R10-5** + the a11y/BP fix + accepted-LCP + keep-branch-open. Audit trail: `.superpowers/sdd/t10-plan/`. **T10→DONE; branch stays OPEN (no merge — owner's call, for the late T-GLB/T-Audio swap-ins); NEXT→T-GLB/T-Audio.**
- **2026-08-27 — T10 QA — PASS.** Prod-build packaging QA (via `vite preview` :4173): `document.fonts` = Space Grotesk only, `?lab` absent at `/` and `/?lab`, mint glow + Identity phrases + no tooltip, both canvases present, favicon + js.map served, **zero console errors**, CLS 0. **Lighthouse mobile (real headless Chrome, `lighthouse@12`): Perf 96 (≥90) · A11y 100 (≥95) · BP 100 (=100) · CLS 0** — the four target audits (`aria-prohibited-attr`, `color-contrast`, `errors-in-console`, `valid-source-maps`) all PASS (were FAIL). **LCP 2.3s accepted** (owner ruling — eager `three`; lazy-load = future perf ticket; Perf 96 passes). On-device residuals (true-60fps/10s-rAF, 50-feed heap, reduced-motion-on-device, migration 2nd-leg, ~30s GIF, SR spot-check) documented with instructions + code/unit evidence in `docs/qa/README.md` (Browser pane freezes rAF; Claude-in-Chrome unavailable this session). Full detail: `.superpowers/sdd/t10-plan/progress.md`.
- **2026-08-27 — T9 DONE.** Hardening — a11y, reduced-motion, memory, mobile (SPEC §12/§13/§15). Ticket commits `97a7d6e..89f740f` (6). New **pure** `pet/a11y.ts`: `createPhraseThrottle` (leading + trailing-coalesce; injected `nowMs`; zero imports/DOM/clock/random — grep-verified) + `announcePhrase` executor (lazy, visually-hidden **polite `aria-live`** region, `aria-atomic=true`, on `document.body`) — **pre-created EMPTY at `createBytePet` construction** (whole-branch-review Important-1: registers the region with the AT ≫1s before the FIRST feed so that first announce isn't dropped) and **torn down in `destroy()`** (`teardownAnnouncer`). Wired at the ONE `handleEnterRetyping` seam (after `next`, before the `reducedActive` split) so a reward retype announces the phrase verbatim (§10) for hero AND footer, full AND reduced — and the **entrance never announces** (it types `entering`, which never routes through that seam; the static headline #1 is already read on load). **Keyboard path (§12):** a real reveal-on-focus `<button data-feed-byte>Feed Byte</button>` in `#hero` — visually-hidden until `:focus-visible`, then revealed `position:fixed` at ≥44×44 with a 2px `--accent` ring (skip-link pattern; `main.ts` wires its click → `feed`, hides it `[hidden]` on the no-WebGL floor); `feed()` now sends `POINTER_DOWN` (wake) **then** `feeder.feed()`, and `onPointerDown` is collapsed to `feed(e.clientX,e.clientY)` — ONE wake-then-feed entry, pointer byte-identical (no double-send; the order is load-bearing — `fsm.test.ts:646` ignores FEED while sleeping). **Reduced-motion audit:** `main.ts` drops the explicit `reducedMotion` from the `createBytePet` call so the module self-detects **LIVE** via its own `gsap.matchMedia()` else-branch (matching `hero.ts`/`manifesto.ts`/`reveals.ts`/`grain.css`; R-T4-7's "survives a live OS flip" now real) — main's own boot `reducedMotion` still keys the one-time `willByteType`/`runEntrance` entrance branch; `scene.ts` never read `reducedMotion` (grep-confirmed) so passing `undefined` is inert; verify sweep (parallax off / reveals instant / migration simplified / blink stays / grain frozen) confirmed. **Memory:** the 2× per-frame `new THREE.Color` in `applyTheme`'s lerp `onUpdate` are hoisted — endpoints built once per call, one reused scratch `themeLerpColor.copy(from).lerp(to,p)` per frame (+ `setDimmed`'s `dimColor` scratch); copy contract verified (`rig.setBodyColor`→`source.body.color.set()`, rig.ts:470) + guarded by 2 new `rig.test.ts` tests; the 50-feed heap is QA-confirmed leak-free (scene mesh/points **6→6**, front-layer children → **1**, glyph queue 0). **Mobile:** ≥44×44 touch targets — `.nav__theme-toggle`/`.nav__eq-toggle` 40→44 (`min-inline/block-size`, icon glyph + EQ bars untouched), `.footer__email` `padding-block` → ~46px (a flex child, so block padding builds a real target); no cursor pills on touch + no horizontal overflow re-verified. Canvases were **already** `aria-hidden`+`pointer-events:none`+non-focusable (scene.ts:262, verified — no change). `fsm.ts`/`retype.ts`/`anchor.ts` + the new `a11y.ts` throttle core stay **PURE**. **No new deps.** Budgets JS **209.90KB gz** / CSS **3.41KB gz** (≪ 280/20; +0.49/+0.12 vs T8). Build/lint/format/`vitest` (**243/243**, +14: 9 throttle, 2 rig copy-contract, 3 region-lifecycle) green. Via **subagent-driven-development**: 5 tasks + per-task Opus reviews (Task 2 recovered from a mid-edit machine-sleep crash — working tree ADOPTED + verified vs brief + committed, not reset — Ruling) + **Opus whole-branch review** (Ready-with-1-fix: the a11y first-announce region pre-create) + 1 fix wave `89f740f` + scoped re-review (all addressed) + **controller browser QA — PASS**. DECISIONS **D-17**; the P-2 halo sprite stays deferred (owner/T10 polish). Full audit trail: `.superpowers/sdd/t9-plan/progress.md`. **T9→DONE; branch stays OPEN (no merge; T10); NEXT→T10.**
- **2026-08-27 — T9 QA (controller browser pass) — PASS.** Vite dev @ :5180 (HEAD `89f740f`), via a temporary uncommitted `window.__byteQA` + `stepGsap` hook (`gsap.updateRoot(clock)+scene.render(dt)`, seeded from `gsap.ticker.time`) driven with **microtask yields — 3 hops/step** (a single hop couldn't cross `enterAndType`'s drop-in `await`, stalling the entrance; three hops let the awaited drop-in resolve mid-drive) + programmatic `theme-toggle` click; **reverted after** (`git checkout src/pet/createBytePet.ts` → tree clean at `89f740f`, no stray `__byteQA` in `src`; 243 tests + lint + format + build re-confirmed green). Proven: **canvases** both `aria-hidden`/`pointer-events:none`/non-focusable; **live region pre-created empty** at construction (`polite`/`atomic`, in `<body>`); **FIRST reward feed announces** "I SHIP PRODUCTS", then "IDEAS → SHIPPED" / "FEED BYTE" (verbatim §10, cycle advancing) while the **entrance stays silent**; **keyboard feed** — Tab reveals the `Feed Byte` button (2px `--accent` ring, 115×44, fixed), click → dash→eat→retype→announce→idle; **`feed()` wakes** a sleeping Byte (`sleeping`→`waking`→feed loop); **50-feed heap** stable (front children→1, mesh 6→6, queue 0, peak 7); **mobile 390×844 (coarse, maxTouch 5)** — 0 cursor pills, no overflow, hero lines fit, touch targets theme/sound 44×44 + email 175×47; **768×1024** no overflow + `pointer:fine`; **both themes** at 1440/390 (+ 768 dark) with theme reaction (Byte stretch + dark mint glow / light glow-off). **Zero console errors** throughout (entrance + 53 feeds + keyboard + heap). Tooling caveats (deferred on-device to **T10**, code+unit backed): `prefers-reduced-motion` behavior not OS-emulable in-pane (Byte's live self-detect + reduced branches unit-tested + 4-review-verified; announce fires before the `reducedActive` split → both modes); a true 60fps 10s rAF sample (pane freezes rAF). Full evidence: `.superpowers/sdd/t9-plan/progress.md`.
- **2026-08-26 — T8 DONE.** Migration, theme reaction & Style-Lab complete (SPEC §6/§7/§11/§10/§12). Ticket commits `6c92333..eb38c80` (10). Byte travels hero↔footer down a right-margin lane following scroll, retypes the footer CTA, happy-spins when fed mid-travel, reacts to theme toggles, and the Style Lab is complete. New **pure** `pet/anchor.ts` `pickAnchor(hero,footer,viewport)` (+7 tests, zero imports). FSM (`fsm.ts`/`types.ts`): reachable **`traveling`** + `MIGRATE`/`ARRIVED` + feed-while-traveling routing `eating--ATE-->traveling` with `feedFromTraveling` provably cleared on ATE/ARRIVED/safety-cap (+15 tests → **229**). `createBytePet.ts`: switchable **active-home** (`setHomeAnchor`; per-home caret/`data-byte-line`/cycle/`phraseIndex`); **footer retype REUSES the single `createRetype`/`onTick` engine** (carry-forward #1 — NO second typing path); scroll-driven **reversible** migration lane (live `pickAnchor` each tick drives lane point + arrival, so a mid-trip reversal re-homes — R8-2); feed-while-traveling **eat + 360° `rotation.y` spin, no retype**; theme reaction `applyTheme(t,{animate})` ~400ms Body/Glow lerp (proxy→`rig.setBodyColor`) + full-height `scale.y` stretch (~1.18×`unitPx`, ROOT) + dark phosphor, single `themeWhoosh` (public wrapper); `setGlowAccent`/`setPhrases` handles (`currentGlowAccent` per-instance). `scene.ts`: animated lights lerp `setTheme(t,durationS)` (lights-only, R-T4-5; kill-before-restart). `page/footer.ts`: bounded SplitText revert (caret geometry, best-effort). `page/lab.ts`: glow(4)+phrase-set(3) axes + copy-locked-config button + `RADIO_GROUP_NAME`→`TYPE_RADIO_GROUP_NAME`. `main.ts`: `<html>` MutationObserver (inside WebGL block) → `setGlowAccent(getComputedStyle('--glow'))` / `setPhrases(identity|punchy|combined)` on boot+change; memoized cursor `setLabel`; dropped vestigial `cursor?` from `initSoundControls` (`hero.ts` sig+call). Single-source `GLYPH_KINDS` (`glyphs.ts`, union derived). `fsm.ts`/`retype.ts`/`anchor.ts` stay **PURE** (grep-verified in the whole-branch review). **No new deps.** Budgets JS **209.41KB gz** / CSS **3.29KB gz** (≪280/20; +2.07 vs T7). Build/lint/format/`vitest` (**229/229**) green. Via subagent-driven-development: **8 tasks + per-task reviews (T1 sonnet; T2 opus; T3 opus +1 fix round; T4 sonnet +1 fix round; T5/T6/T7/T8 sonnet) + Opus whole-branch review (Ready-with-fixes: 2 Important + minors) + 1 final fix wave `eb38c80` (traveling reacquire-kill; reduced-motion migration in-band gate; glow-accent encapsulation) + scoped re-review (all addressed, no new breakage) + controller browser QA (PASS, both themes × 3 viewports)**. One account session-limit blip mid-T4 (Opus impl died pre-write at `c0fa02e`; tree verified clean; re-dispatched Sonnet). DECISIONS **D-16**; rulings **P-1/P-2/P-3, R8-1..R8-4**. Full audit trail: `.superpowers/sdd/t8-plan/progress.md`. **T8→DONE; branch stays OPEN (no merge; T9–T10); NEXT→T9.**
- **2026-08-26 — T8 QA (controller browser pass) — PASS.** Vite dev @ :5180 (HEAD `eb38c80`), via a temporary uncommitted `window.__byteQA` + `stepGsap` hook (`gsap.updateRoot(clock)+scene.render(dt)`, seeded from `gsap.ticker.time`, driven with **microtask yields** so `enterAndType`'s drop-in `await` resolves mid-drive) + programmatic `scrollingElement.scrollTop` (native scroll works; Lenis frozen); **reverted after** (`git checkout src/pet/createBytePet.ts` → tree clean at `eb38c80`; 229 tests + build + lint + format re-confirmed green). Proven at **1440×900 dark**: entrance live-types `FULL-STACK / +AI`→`idle`; **R8-1 one-caret** (hero visible / footer hidden, FLIPS on migration); **migration hero→footer** (lane travel → `active:footer`, re-home); **footer retype** `feed()`→`dashing→eating→retyping`→`SAY / HELLO` (SPEC §10 exact) with hero text untouched (single engine, no cross-home bleed); **feed-while-traveling** `traveling→dashing→eating→traveling` + `rotation.y`≈2π spin, NO retype; **theme reaction** `scale.y` 104→123(=×1.18)→104 stretch + glow emissive 0.6↔0 (dark phosphor / light off); **lab wiring** `data-glow=amber`→glow emissive `#ffb454`, `data-phrase-set=punchy`→hero cycle→`["MAKE IT","MOVE"]`; **`/?lab`** shows all 4 axes + "Copy locked config"→logs `{type:grotesk,glow:mint,phraseSet:identity,tooltip:off}` + "Copied ✓". **768×1024** and **390×844 (mobile/touch)**: entrance+migration+caret-flip, light-theme glow-off, **no horizontal overflow / both hero lines fit**, no cursor pills. **Zero console errors** at every viewport/theme; dark+light hero screenshots captured. Tooling caveat (logged, not a blocker): the **2nd-leg migration lane traverse** (footer→hero re-home) is not in-pane-drivable — after an arrival's `gsap.killTweensOf(position)` the lane `quickTo` re-creates via `resetTo` (a live-ticker behavior `gsap.updateRoot` stepping can't advance), PROVEN functional out-of-band (the re-created tween reaches its exact target when its playhead is forced) ⇒ real-app-correct + reviewer-hand-traced; deferred like reduced-motion/60fps/50-feed-heap to on-device **T9/T10**. Full evidence: `.superpowers/sdd/t8-plan/progress.md`.
- **2026-08-25 — T7 DONE.** Sound & custom cursor (SPEC §9/§8.3/§7). Ticket commits `f15f18e..06f689e` (8). A swappable **`SoundEngine`** (`pet/sound/SoundEngine.ts`: `unlock/setEnabled/enabled/play/setMaster`, `Cue`=typeTick|eatA|eatB|spawnPop|themeWhoosh|wakeBoing|chirp, + `silentSoundEngine` no-op) + a zero-asset **WebAudio synth** (`webAudioSynth.ts` `createWebAudioSynth()`: lazy AudioContext, first-gesture `unlock()`, ON-after-gate with `byte:sound` localStorage persist, `[0,1]` master clamp, degrade-to-silent, `.catch()`-guarded async `resume()`; **`webAudioSynth.test.ts` 14 tests, mock AudioContext**). **Howler NOT added** (D-07 later; engine-agnostic call sites — only `sound.play`). Cues wired (`createBytePet.ts`/`feed.ts`): `typeTick` at the per-edit retype detector under ONE SHARED throttle with the caret spark (`TYPE_TICK_EVERY=3`; skips `total===0` + `reducedActive`; gate kept `retyping||entering` → the **ENTRANCE types audibly**, carry-forward #1); `eatA/eatB` alternate via `tl.call` at the 2 chomps (single `eatA` reduced); `spawnPop` on toss; `themeWhoosh` in the `setTheme` wrapper (NOT `applyTheme`/construction); `wakeBoing` on wake; `chirp` on peek (R7-2). Custom cursor `lib/cursor.ts` `initCursor()` (dot→pills FEED/TOGGLE/OPEN, gsap.quickTo, **no pills on touch** via `matchMedia('(hover:hover) and (pointer:fine)')`; **`cursor.test.ts` 10 tests**) + a `main.ts` zone resolver (`a[href]`→OPEN, `#theme-toggle`/`[data-eq-toggle]`→TOGGLE, `#hero`→FEED). Nav **EQ 3-bar toggle** + `(click to enable sound)` gate label + one-time capture `pointerdown`+`keydown` unlock (`index.html`/`page/hero.ts` `initSoundControls`/CSS; EQ animates only `enabled&&unlocked`; reduced-motion static; **`hero.sound.test.ts` 7 tests**). Style-Lab **tooltip on/off axis** (`page/lab.ts` flips `data-tooltip`) + a `still hungry` runtime tooltip (`main.ts`, eaten≥3 AND `data-tooltip='on'`, CLS-safe, `<html>` MutationObserver). `fsm.ts`/`retype.ts` **PURE** (untouched — confirmed absent from the diff). **No new deps.** Budgets: JS **207.34KB gz** / CSS **3.26KB gz** (≪ 280/20; +1.99 vs T6). Build/lint/format/`vitest` (**207/207**, +31) green. Via subagent-driven-development: **6 tasks + per-task reviews (T1 opus core; T2 sonnet; T3 opus; T4 opus; T5 sonnet; T6 opus) + 2 fix rounds (T1 async `resume()`-rejection handler + test; T4 keyboard-only unlock + test) + scoped re-reviews + Opus whole-branch review (Ready=YES; all 12 deferred minors carried) + controller browser QA (PASS)**. One account session-limit blip mid-T4 (agent died pre-write; tree verified clean at `8619f28`; re-dispatched). DECISIONS **D-15**; rulings **R7-1..8**. Carry-forwards resolved: #1 entrance-typeTick DONE; #5 GLYPH_KINDS LEFT (T7 didn't touch that list, R7-7). Audit trail: `.superpowers/sdd/t7-plan/progress.md`. **T7→DONE; branch stays OPEN (no merge; T8–T10); NEXT→T8.**
- **2026-08-25 — T7 QA (controller browser pass) — PASS.** Vite dev @ :5180 (HEAD `06f689e`), Web-Audio-counter instrumentation (browser-side JS only — **no source edits, tree stayed clean at `06f689e`**; 207 tests + build + lint + format re-confirmed green independently). Proven: **no audio before the gate** (`{osc:0,resume:0}` pre-gesture); **gate opens + cues fire** (first gesture → `resume:1`; hero feed → spawnPop voice; theme toggle → themeWhoosh voice); **EQ mutes/unmutes + persists** (mute → `is-on`/`aria-pressed` off, `byte:sound='0'`, muted feed → 0 voices; reload with `='0'` → EQ fresh-loads muted, first gesture unlocks (`resume:1`) but stays silent `osc:0`); **gate label** shows pre-unlock, removed on unlock; **cursor pills** FEED/`#hero`, TOGGLE/EQ+theme, OPEN/`a[href]`, null(hidden)/empty; **touch (375×812, coarse, maxTouch=5) → 0 cursor nodes / no pill** while EQ+gate still work; **tooltip** hidden by default + stays hidden at `data-tooltip='on'` with <3 feeds (AND-gate + MutationObserver); **zero console errors** throughout. Ticker-driven cues (entrance `typeTick`, `eatA/eatB`, `chirp`/`wakeBoing`) + reduced-motion per-char silence NOT pane-reproducible (pane forces `visibilityState:hidden` → gsap ticker frozen; reduced-motion not OS-emulable in-pane) — covered by unit tests + Task-3 source-trace (typeTick at the per-edit detector, gate `retyping||entering`, first emit on the 3rd char, skips under `reducedActive`) + runtime proof the engine DOES produce voices on `play()`; formal on-device T9/T10. Full evidence: `.superpowers/sdd/t7-plan/progress.md`.
- **2026-08-24 — T6 (ENTRANCE half) DONE — T6 COMPLETE.** The SPEC §8.1 entrance, completing T6 by **REUSING** the retype engine (do-not-write-a-second-typing-path). Ticket commits `237389a..c7b5196` (5). On boot: a preloader (blinking caret + **%**, gated on `document.fonts.ready` raced against a bounded timeout AND a MIN floor → resolves in `[900, 4000]ms`, **never hangs**) lifts; then Byte **drops in with a bounce and LIVE-TYPES phrase #1 (`FULL-STACK`/`+AI`) from EMPTY** by reusing `createRetype`/`renderRetype` + the single `onTick` retype-driver; nav/micro-label/scroll-cue stagger in; **ZERO CLS**. New file: **`page/preloader.ts`** — `runEntrance(deps)` (%, gate, lift, `await bytePet?.enterAndType()`, chrome stagger; all DOM lookups guarded; reduced/instant). PURE primitives (`237389a`): `fsm.ts`/`types.ts` — `hidden`/`entering` made reachable via `initialState` config + new `SHOWN`(hidden→entering)/`ENTERED`(entering→idle) events + `enteringMs`(8000) safety cap (the exact REACHED/ATE/RETYPED mirror; `initialState` defaults `'idle'` so every existing test + the non-entrance path is unchanged); `retype.ts` — a pure `createRetype().reset(lines)` re-seed so the entrance types from `['','']` (**fsm.test.ts +13, retype.test.ts +2 → 176**). `createBytePet.ts` (`c0b1fa3`): hidden-start construction (hide Byte+shadow; clear headline to empty ONLY when not reduced — else keep static phrase #1); async **`enterAndType()`** (full: `SHOWN` → `await` a `bounce.out` drop-in (scaling `pose.scale`, NOT the `unitPx` root) → `reset(['',''])`+`enqueue(cycle[0])`+`retypeActive` → the reused `onTick` driver types + follows the caret → `ENTERED` on completion; reduced: place at anchor instantly, `SHOWN`+next-tick `ENTERED`; resolves via a one-shot `entering→idle` detector); the `onTick` retype gate generalized to `retyping||entering` (fires `ENTERED` vs `RETYPED` by state) + the caret-rest write inverse-gated. `main.ts`/`hero.ts` (`c7b5196`): `willByteType = webgl && !reducedMotion` suppresses the hero SplitText reveal ONLY when Byte live-types; `entrance: webgl` (R-T6b-9 — `enterAndType` branches full/reduced INTERNALLY via `reducedActive`, so `hidden→entering→idle` runs in both modes); `void runEntrance(...).catch(...)` on both paths (first-paint-unblocked, rejection-safe); the no-WebGL early-return restructured so the preloader still lifts to the static headline. **No new deps.** Budgets: JS **205.35KB gz** / CSS **2.86KB gz** (≪ 280/20; +0.93 vs T6a). Build/lint/format/`vitest` (**176/176**) green. Via subagent-driven-development: **4 tasks + per-task reviews (T1/T2 opus, T3 sonnet + 1 fix round + scoped re-review, T4 opus) + Opus whole-half review (Ready=YES) + controller browser QA (PASS).** The one T3 fix round resolved a real CLS finding the review caught: the preloader **%** counter re-centered at the 9→10 / 99→100 digit-count changes (`tabular-nums` fixes per-digit width but not string length) → reserved `.preloader__pct` `min-width:3ch` + zeroed the inner `letter-spacing` (the `ch` unit ignores tracking, which was overflowing "100"). Ruling **R-T6b-9** (ruled mid-execution): `entrance: webgl` not `willByteType`. DECISIONS **D-14**; rulings R-T6b-1..9. Full audit trail: `.superpowers/sdd/t6b-plan/progress.md`. **⛳ T6 COMPLETE — owner play-test stop reached: NO merge (branch open for T7–T10); TICKETS T6→DONE; NEXT→T7.**
- **2026-08-24 — T6 (entrance) QA (controller browser pass) — PASS.** Vite dev @ :5180; via a temporary uncommitted `window.__byteQA` + `stepGsap` (`gsap.updateRoot(clock)+scene.render`, seeded from `gsap.ticker.time`) hook to drive the frozen-rAF pane (reverted after via `git checkout src/pet/createBytePet.ts`; tree back to `c7b5196`; build + 176 tests + lint + format re-confirmed green). Proven: preloader %→lift (never-hang); Byte drop-in + live-type phrase #1 from empty (state `entering`→`idle`, caret 12 distinct positions, `phraseIndex` stays 0); **CLS-0** (`#hero-headline` height a single value — desktop 201.59 / mobile 106.57 — across empty→typed→idle); feed→retype reward loop intact (`idle→dashing→eating→retyping→idle`, phrase #2 `I SHIP`/`PRODUCTS`, idx 0→1, CLS-0); guaranteed-early peek (`idle→peeking→idle`); both themes (dark shows mint phosphor Glow); mobile 375px no doc/per-line overflow; **zero console errors**. Reduced-motion instant + no-WebGL/JS-off floors verified at task/review level (not OS-emulable in-pane); formal 60fps + 50-feed heap → on-device T9/T10. Full evidence: `.superpowers/sdd/t6b-plan/progress.md`.
- **2026-08-24 — T6 (RETYPE REWARD half) DONE.** The signature reward (SPEC §6; D-03 — a custom per-char span engine, NOT SplitText). Ticket commits `f5222ea..53210cd` (5). After a feed's eat completes, Byte glides to the headline end and drives a blinking DOM caret to backspace the current 2-line phrase and retype the NEXT (Identity set, cycling) with **ZERO layout shift**. New `pet/` files: **`retype.ts`** — a **PURE** engine `createRetype`/`buildRetypeSchedule` (delete BOTTOM line then TOP as ONE accelerating ~26ms/char backspace run; type TOP then BOTTOM ~40ms/char ±12ms jitter via an **injected** `random` — imports NEITHER gsap NOR three, no wall-clock/`Math.random`, SPEC §4.3) + `renderRetype` (per-char `.byte-char` spans + a `getBoundingClientRect`-delta-positioned caret; no container reflow) + **`retype.test.ts` (24 tests — the tested core: delete-order, total-typed, caret monotonic-during-delete/advance-during-type, no-overflow, jitter-in-bounds, accel, `isBusy`)**. FSM (`fsm.ts`/`types.ts`): `retyping` made reachable by rerouting `eating --ATE--> retyping` + a new `RETYPED` event (`retyping→idle`) fired by the retype-driver + a `retypeMs`(4000) safety cap — mirrors T5's REACHED/ATE (**fsm.test.ts 49→54**). `createBytePet.ts`: the retype-driver — tags the 2 line els (`data-byte-line`) + a LIVE-resolved `data-byte-caret` caret, `handleEnterRetyping()` (pass-through / reduced instant-set / full), `onTick` steps the engine off a monotonic clock → `renderRetype` → `quickTo` Byte→caret follow → per-edit box-shadow spark → fires `RETYPED` on completion; reduced-motion (`reducedActive`) → instant text set, no glide/spark; caret blink is CSS (stays under reduce, SPEC §12). `page/hero.ts`: reverts the load-reveal SplitText after it plays. `main.ts`: passes `phrases.identity`. **No new deps.** Budgets: JS **204.42KB gz** / CSS 2.60KB gz (≪ 280/20; +1.6KB vs T5). Build/lint/format/`vitest` (**161/161**) green. Via subagent-driven-development: **4 tasks + per-task reviews (T1 opus core; T2/T3 sonnet; T4 opus) + Opus whole-half review (ready: yes) + 1 final-review fix wave + scoped re-review — all clean.** Fix wave resolved a real bug the browser QA caught: the hero SplitText **revert restores CLONED** headline children, orphaning the captured `caretEl` ref → the caret never moved + Byte glided to (0,0); fixed by resolving the LIVE caret (`[data-byte-caret]` query) at every use + `getBoundingClientRect`-delta caret geometry (R-T6a-9 also fixed a reduced-motion recipe defect: `enqueue; step(0); step(MAX)`, not a lone `step(MAX)`). DECISIONS **D-13**; rulings R-T6a-1..9. Full audit trail: `.superpowers/sdd/t6a-plan/progress.md`. **⚠️ T6 is NOT fully DONE — the ENTRANCE half (`page/preloader.ts` + `runEntrance()`, SPEC §8.1) is a SEPARATE next session; TICKETS T6 stays OPEN; no owner play-test this session.**
- **2026-08-24 — T6 (retype) QA (controller browser pass) — PASS.** Vite dev @ :5180; via a temporary uncommitted `window.__byteQA` + `stepGsap` (`gsap.updateRoot`) hook (reverted after; tree back to `53210cd`; build+161 tests re-confirmed).
  - **CLS-0 (DoD) — PROVEN:** `#hero-headline` `getBoundingClientRect().height` byte-identical across `idle→dashing→eating→retyping`→`idle` — including the fully-deleted `""` frame mid-retype (`distinctHeights` = a single value each run); `min-height: calc(1.05em*2)` enforced, both `.hero__line` `display:block`, 2 lines stacked.
  - **Full retype (DoD) — PROVEN:** deletes `+AI` then `FULL-STACK` to empty, types `I SHIP` then `PRODUCTS` → phrase #2 "I SHIP PRODUCTS"; `RETYPED` fired ~800ms in (no 4s `retypeMs` stall). Zero console errors throughout.
  - **Caret + Byte-operates-the-caret (post-fix):** live `[data-byte-caret]` transform non-empty at rest (line-end) and takes **24 distinct positions** tracking the delete/type (it IS the live DOM node — the detach is gone); Byte's root ranges across the headline following the caret, `finalByte.x`≈`finalCaret` x at the new line-end (not stuck at the corner).
  - **Mobile 375px (carry-forward #3):** all 5 Identity phrases cycled `0→1→2→3→4→0`; every `.hero__line` `scrollWidth ≤ clientWidth`, `documentElement.scrollWidth ≤ innerWidth` (font-size 50.75px @ 375px) — no per-line or page overflow.
  - **Tooling caveats (logged, not blockers):** reduced-motion isn't emulable in-pane (verified at task/review level — the `reducedActive` branch renders the final frame + fires `RETYPED` next tick, no glide/spark); formal 60fps + the 50-feed heap are on-device at **T9/T10**.
- **2026-08-23 — T5 DONE.** Feeding loop. Ticket commits `4d01c2f..b300d4c` (6). Click/tap tosses a code glyph; Byte dashes (banked ±12°, slight overshoot) and eats it into `rig.mouthWorld()` over 2 chomps with 4–6 `currentColor`(amber) particles; the footer **`FED N GLYPHS`** counter increments; the live-glyph queue caps at **3** (oldest pops); touch works; every eaten/popped glyph's geometry+material is disposed. New `pet/` files: **`glyphs.ts`** (`makeGlyph('{'|';'|'>'|'*'|'+'|'=')` — bevel-extruded `THREE.Shape` primitives, **no `typeface.json`** — plus a **pure** `createGlyphQueue` decision-core: cap 3, eaten total, `onEat` fan-out, no gsap/three) + **`glyphs.test.ts` (24 tests — the ticket's tested core: 6-kind geometry no-throw, cap 4→3 pops oldest, onEat increments, `markEating` protects the eat target, `next()` ordering, register-many)**; **`feed.ts`** (`createFeeder(rig, scene, fsm, { prefersReducedMotion, unitPx })`: toss 450ms arc+spin+landing-squash → banked distance-scaled dash → `Eat` two chomps into the mouth → particles → count → per-glyph dispose; **`fsm.onEnter`-driven** dashing/eating; drains the queue via a legal `FEED`; reduced-motion → instant/fade, minimal particles; `dispose()` frees all live glyphs+particles). Modified: **`fsm.ts`/`types.ts`** — new feeder-fired events **`REACHED`** (dashing→eating) / **`ATE`** (eating→idle) making `eating` reachable, generous safety caps `dashMs`(→**1200**)/`eatMs`(**1500**), and the **MUST-FIX (carry-forward #1): `isSleepEligible` denylist → awake-resting-state allowlist `{idle, curious, invited, peeking}`** (Byte can no longer sleep mid-feed; `peeking` stays eligible) + `curious`/`invited`/`idle` `POINTER_DOWN` sleep-accum resets (**fsm.test.ts 39→49**). **`createBytePet.ts`** — builds+wires the feeder (`prefersReducedMotion: () => reducedActive`), `feed()`→`feeder.feed()` (no bare `send('FEED')`), `dispatch()` no longer plays `Dash`/`Eat` + **skips `clearLean()` for dashing/eating** (source fix for the bank conflict) + a **stale-transition guard** `if (fsm.state() !== state) return;` (nested re-entrant transitions), `onTick` **root-placement handoff** (feeder owns `rig.object3d.position` during dashing/eating; **no-snap reacquire glide** back to the headline anchor on return; **shadow follows the actual root**), `BytePetHandle.onEat`, `feeder.dispose()` in teardown; also folded the two parked T4 minors (hint-tween single-slot prune; `playUnlessReduced` doc-comment fix). **`main.ts`** — wires `[data-fed-counter]` (in the **footer**, SPEC §8.6 — the ticket's "hero.ts" hint was stale) to `onEat`. **Blob-shadow vertical tuning UNCHANGED** (carry-forward #4 re-checked: the dash is horizontal locomotion; only the tossed glyph arcs vertically — R-T5-7). **No new deps** (`Shape`/`ExtrudeGeometry`/`Points` ship in `three`; gsap already in). Budgets: JS **202.86KB gz** / CSS **2.60KB gz** (≪ 280/20; +11KB vs T4's 191.65). Build/lint/format/`vitest` (**132/132**) green. Executed via subagent-driven-development: **4 tasks + per-task reviews (Task 1 sonnet; Tasks 2/3/4 opus) + Opus whole-branch review (ready: yes) + 2 fix rounds + scoped re-reviews** — all clean. Fix loops resolved real bugs pre-commit: Task 3 `playSatisfiedWiggle` tweened the root scale to absolute `1`/`1.08` while it rests at `unitPx` (first eat shrank Byte to a speck) → pulse made relative to `unitPx`; Task 4 idle-rechain **re-entrancy** (feeder listener re-sent `FEED` mid-`enter('idle')`, so stale `dispatch('idle')` replayed `Idle` over the fresh Dash + fought the bank) → the stale-transition guard. Minors carried (see NEXT→T6). DECISIONS **D-12**. Full audit trail: `.superpowers/sdd/t5-plan/progress.md`.
- **2026-08-23 — T5 QA (controller browser pass) — PASS.** Vite dev @ :5180; dark + light + mobile 375×812. Pane still throttles rAF (`visibilityState:hidden` → gsap's ticker frozen), so the tween-driven eat was driven via a **temporary, uncommitted `window.__byteQA` handle** (`{ scene, rig, source, fsm, feeder, feed, liveCount, frontChildren, forceRender }` + a `stepGsap` that advances gsap's root clock with `gsap.updateRoot`) + forced renders — **reverted after** (working tree back to `b300d4c`; build + 132 tests re-confirmed green).
  - **Queue cap (DoD):** feed ×4 rapid → **`liveCount()===3`** (oldest popped); feed ×3 → 3.
  - **Counter (DoD):** `FED 0 GLYPHS` → after 3 feeds run to completion via `stepGsap` → **`FED 3 GLYPHS`**; `liveCount` 0.
  - **Dispose (DoD proxy):** front-layer children returned to **1** (shadow only) after all eats → every glyph mesh + particle geo/mat removed & freed; no accumulation. (Formal 50-feed heap is on-device at **T9/T10**.)
  - **Touch (DoD):** mobile emulation (375×812, UA mobile, `maxTouchPoints:5`) — a real `pointerType:'touch'` `pointerdown` on the hero → glyph spawns, `idle→dashing`. **No cursor pills** (0 cursor elements — the cursor UI is T7).
  - **Themes:** dark shows Byte's **mint phosphor Glow**; light shows the clay body + blob shadow; amber glyphs weave with the "FULL-STACK" letters (mobile shot captured). **Zero console errors** throughout (load + feed loop + theme toggle + forced renders + touch).
  - **Tooling caveats (logged, not blockers):** reduced-motion isn't emulable in-pane (verified at task/review level — the feeder's `prefersReducedMotion` branch + `createBytePet`'s `reducedActive`); formal 60fps + the 50-feed heap are on-device at **T9/T10**.
- **2026-08-23 — T4 DONE.** Placeholder bot, rig adapter & idle brain (FSM). Ticket commits `ca9de74..7866155` (6). New `pet/` files: `fsm.ts` (**pure**, config-injected FSM — no gsap/three, time only via `tickTimers(dtMs)`; full SPEC §6 state union; at most one transition/call, state-specific exit before the idle→sleeping overlay) + **`fsm.test.ts` (39 tests — the ticket's tested core)**; `placeholderBot.ts` (`createPlaceholderBot`→`RigSource`: `RoundedBoxGeometry` robot sized to the live headline `unitPx`, named `Body`+`Glow` materials, eye/`Mouth` nodes, internal `byte-placeholder-pose` group so clip motion never fights root placement, `clips:{}`); `rig.ts` (`createPetRig`→`PetRig`: GSAP-faked in-place clips Idle/Hop/Dash/Eat/Sleep/Wake/Peek, `setLook` proportional yaw ±0.35 via `quickTo`, `setBodyColor`/`setGlow`, `mouthWorld`, `dispose` kills all tweens+bot geo/mats; GLB+`AnimationMixer` branch left as a commented seam for T-GLB); `createBytePet.ts` (orchestrator, **owns** scene+ticker per SPEC §4.4). Extended `types.ts` (`PetState`/`PetEvent`/`PetFSMConfig`/`PetFSM`/`ClipPlayOptions`/`PetRig`/`PetOptions`/`BytePetHandle`), `scene.ts` (`addToPet`/`addToFront` layer helpers + `bodyColorForTheme`/`DEFAULT_GLOW_ACCENT`; **`setTheme` now lights-only** — blanket material repaint removed), `shadow.ts` (`MAX_HOVER_HEIGHT` retuned 140→**64** vs the bot's real hop apex; monotonic-test arrays updated), `main.ts` (delegates to `createBytePet`; **`?glcube` rig + `window.__byte*` removed**). Idle brain: hard-step blink (survives reduced-motion, stops in curious/sleeping), 4–8s micro-behaviour scheduler (glance/hop/slide/peek; a peek in the first ~10 idle s), peek-behind occlusion (layer flip at apex), curious lean + invited `(click to feed Byte)` hint (once per `localStorage`), sleep dim + wake, cursor-tracked eyes; reduced-motion via `gsap.matchMedia()`; theme via `rig.setBodyColor`+`setGlow(dark, mint)`. **No new deps** (`RoundedBoxGeometry` ships in three; DECISIONS **D-11**). Budgets: JS **191.65KB gz** / CSS **2.60KB gz** (≪ 280/20; three ≈ 131KB of it). Build/lint/format/`vitest` (98/98) green. Executed via subagent-driven-development: **4 tasks + per-task reviews (Opus on the FSM core + on createBytePet) + Opus whole-branch review + 1 final fix wave + scoped re-reviews** — all clean. Fix loops resolved real bugs pre-merge: Task 3 cross-clip pose residue (idle→dash→idle left Byte stuck leaning) + degenerate `setLook` atan2 on the coplanar z=0 scene; Task 4 peek-only scheduler (wander/sleep/wake were unreachable) + per-tick pointer send nullifying the FSM's "PEEK doesn't delay sleep" + reduced-motion perpetual Idle/Sleep bob + approach-during-peek starving `curious`. Minors parked/carried (see NEXT→T5). Full audit trail: `.superpowers/sdd/t4-plan/progress.md`.
- **2026-08-23 — T4 QA (controller browser pass) — PASS.** Vite dev @ :5180; both themes @ 1440×900. Pane still throttles rAF, so verified by **forcing renders + `gl.readPixels`** through a temporary, uncommitted `window.__byteQA` handle (reverted after; working tree back to `7866155`, build+98 tests re-confirmed green).
  - **Occlusion (headline DoD) — proven twice.** Pixel readback: with Byte over the "A" of "FULL-STACK", `setBehind(true)` → Byte's clay pixels on **#gl-back** (rgba 131,122,106,255) with **#gl-front** empty (a=0); `setBehind(false)` → the exact reverse (front has Byte, back empty) — Byte cleanly moves layers, DOM headline always between. Screenshots: **eyes/head above the letterforms mid-peek with the body behind** (`setBehind true`) vs **body covering "STACK"** (`setBehind false`), same position.
  - **Themes:** light + dark; dark shows `data-theme=dark`, body `#2c2933`, and the **mint phosphor Glow** lit (`emissiveIntensity 0.6`, `#38e8a8`) — the full theme path (scene lights + `rig.setBodyColor` + `rig.setGlow`).
  - **Idle brain (live FSM run):** `sleeping→waking→dashing→idle→curious→invited→dashing` all fired in the real integration; `PEEK` correctly ignored while dashing; FSM reached `sleeping` via elapsed real time and `sleeping` correctly ignored `POINTER_NEAR`. Invited hint present in the DOM with the exact copy `(click to feed Byte)`; once-ever gating (`byte-hint-shown`) wired.
  - **Zero console errors** throughout (load + theme toggle + FSM driving + forced renders).
  - **Tooling caveats (logged, not blockers):** reduced-motion isn't emulable in-pane — verified at task/review level + the implementer's harness (pose stays identity under reduce, blink runs, sleep dims); the 60s-idle heap check + formal 60fps + Lighthouse are on-device at **T10**.
- **2026-08-22 — T3 DONE.** WebGL foundation (two-canvas sandwich). Added the last big locked-stack dep **`three@0.185.1`** (+ `@types/three`; DECISIONS **D-10**). Framework-agnostic `pet/` module: `types.ts` (`SceneOptions`/`SceneHandle`/`ClipName`/`RigSource`); `scene.ts` **`createScene`** — two `WebGLRenderer`s on `#gl-back`/`#gl-front`, one Scene+`PerspectiveCamera`, **THREE-layer front/back occlusion** (`petLayer` obeys `setBehind`, `frontLayer` always front, lights `enableAll()` so both passes stay lit), **pixel-space camera** (fov 30, z=(h/2)/tan(15°), 1 world unit = 1 CSS px @ z=0), DPR-clamp≤2 + resize, hemisphere+key lights, themed `MeshStandardMaterial` + `setTheme` (dark mint emissive), thorough `dispose()`; `motion.ts` **`startTicker`** rides the **same `gsap.ticker`** T2 established (no 2nd rAF) + `visibilitychange` draw-pause; `shadow.ts` blob-shadow sprite + pure height→scale/opacity math; `glbLoader.ts` GLTFLoader+DRACO infra + pure `mapGltfToRigSource` (Body/Glow, Eye/Head, Mouth, named clips; case-insensitive, no-throw, SkinnedMesh-safe; decoder `/draco/` files deferred to T-GLB). `main.ts` wires it behind a **`hasWebGL()` guard** with a `?glcube` dev-only occlusion rig (`window.__byteScene`). Pure seams unit-tested: camera math, shadow math, GLB mapper. Executed via subagent-driven-development: **5 tasks + per-task reviews (incl. Opus on the load-bearing scene task) + Opus whole-branch review (ready: yes) + 1 fix wave + scoped re-review** — all clean. 1 Important fixed (`pagehide` broke bfcache resume → gated on `!event.persisted`). Budgets: JS **185.91KB gz** / CSS **2.60KB gz** (≪ 280 / 20; three ≈ 131KB gz of it). Ticket commits `7478b31..91d9b2d` (6). Full audit trail: `.superpowers/sdd/t3-plan/progress.md`.
- **2026-08-22 — T3 QA (controller browser pass) — PASS.** Vite dev @ :5180; dark + light; 1440×900 & 900×600. Pane throttles rAF (`visibilityState:hidden`), so occlusion verified by **forcing `renderBack()`/`renderFront()` + reading GL pixels** via the `?glcube` `window.__byteScene` handle, not scroll-and-watch.
  - **Occlusion (headline DoD) — proven twice.** Pixel readback: the cube's ~22.3k near-opaque px (maxα=255) render on **#gl-back** when `setBehind(true)` and move intact to **#gl-front** when `setBehind(false)` (back canvas fully empty in front-state); blob shadow stays on **front** both states (maxα≈88 = 0.35 opacity). Visual screenshots: cube **behind** the "STA[C]K" letters (shows through letter gaps) vs **in front** (covers the "A") — same position, `setBehind` flip. Both themes.
  - Stacking: `#gl-back` z-index -1 (above `--paper`, behind `#app`), `#gl-front` z-index 40 (above content, below grain 9999); both fixed / `pointer-events:none` / `aria-hidden`. Camera round-trips (`worldFromScreen`→set→`project` = same screen px). Resize/DPR: on a real `resize` event both renderers `setSize` + camera `aspect`/`z` recompute + `setPixelRatio`≤2; occlusion holds post-resize. Theme toggle re-themes body + emissive (mint in dark). `?glcube`-gating airtight (no handle/cube in production). JS-off/WebGL-missing floor: raw HTML keeps static headline #1, zero canvas markup (canvases JS-created). Zero console errors throughout.
  - **Tooling caveats (logged, not blockers):** the pane's `resize_window` changes the viewport **without** firing a `resize` event (dispatching it / calling `resize()` works — isolated as a tooling artifact, not a bug); visibility-pause (unit-tested in `motion.test.ts`), reduced-motion, and forced-WebGL-off are code/unit-verified (not emulable in-pane). Formal 60fps + Lighthouse = on-device at **T10**.
- **2026-08-22 — T2 DONE.** Typography, sections, scroll & reveals. `phrases.ts` (SPEC §10) + pure `fitsLineBudget`; first runtime deps **gsap 3.15** (core + ScrollTrigger + free SplitText) + **lenis 1.3** (DECISIONS **D-09**). Static section copy (nav/hero/manifesto/work/footer) keeping **CLS-0 + JS-off headline #1**; CSS work-row hovers. **Lenis + ScrollTrigger** on one `gsap.ticker` clock (reduced-motion → native). **SplitText** masked reveals (hero load; manifesto/work/footer on scroll) + hero mouse-parallax + scrubbed manifesto parallax; never-hidden / reduced-motion-instant / full-cleanup invariants page-wide. `?lab` **typography** axis (grotesk/mono/clash). Executed via subagent-driven-development: 6 tasks + per-task reviews + **Opus** whole-branch review (ready: yes) + 1 fix wave + scoped re-review — all clean; **1 minor parked** (reveal `revealed`-flag set at tween-creation → a resize before a scroll-gated section's first scroll skips its entrance; non-blocking, never strands content). Budgets: JS **54.8KB gz** / CSS **2.6KB gz** (≪ 280 / 20); fonts ~99KB (3 families, subset→one at T10). Ticket commits `90b3320..e831f0b` (7). Full audit trail: `.superpowers/sdd/t2-plan/progress.md`.
- **2026-08-22 — T2 QA (controller browser pass) — PASS.** Vite dev @ :5180; both themes across 1440×900 / 768×1024 / 390×844 (+ 844×390 landscape).
  - Zero console errors (all viewports/themes).
  - **No-flash + JS-off floor:** axes `data-theme`/`data-type`/`data-glow` set pre-paint; static headline #1 `FULL-STACK`/`+AI` present; footer #1 `LET'S`/`BUILD`. **Theme toggle** flips + persists across navigations.
  - **Reveals:** hero masked line-rise reaches final state (opacity 1); below-fold sections in expected pending state (visible under JS-off — no CSS `opacity:0` — and under reduced-motion). Lenis active (`html.lenis`).
  - **`?lab`:** no panel at `/`; panel at `/?lab` (legend "Type", 3 accessible radios); live font swap verified via `getComputedStyle` — grotesk→Space Grotesk, mono→JetBrains Mono, clash→Clash Display.
  - **No horizontal overflow** at any tested width (carry-forward `.hero__line` nowrap: static #1 max real line 10 chars, fits; ≤14 budget unit-tested). Lab panel fits within viewport at 390px.
  - **Known edge (minor, deferred):** scroll cue sits ~37px below fold at extreme short landscape (~390px tall).
  - **Tooling caveats (logged, not blockers):** this session's Browser pane throttles `requestAnimationFrame` to 0 (tab reports `visibilityState:hidden` even when fronted) → GSAP-driven motion isn't visually sampled in-pane and a true **60fps** sample isn't reliable here; reveals/parallax verified **structurally + via per-task runtime proofs against gsap 3.15 source**; `prefers-reduced-motion` not emulable in-pane (verified at task-review level: reduce branches create zero tweens + set final state). Formal 60fps is an on-device check at **T10** (SPEC §13); the perf argument rests on transforms/opacity-only + ScrollTrigger rect-caching + the small JS bundle. SDD ledger: `.superpowers/sdd/t2-plan/progress.md`.
- **2026-08-22 — T1 DONE.** Vite+TS app scaffolded (Next.js removed), design tokens (light/dark + type/glow axes), 3 self-hosted font families, no-flash `initTheme()` + tests, CLS-0 layout shells, runtime film grain, theme toggle. Executed via subagent-driven-development: 3 tasks + per-task reviews + Opus whole-ticket review + owner palette lock + fix wave — all clean. **Palette locked (owner):** warm-paper/near-black-ink light, near-black-paper/soft-white-ink dark, accent `#a8451f` light / `#ff8a5c` dark (AA-clean), glow `mint` default. Budgets: fonts ~99KB, built CSS 4.95KB, built JS 2.05KB (all well under). Ticket commits `166413c..ca29d48`. Full audit trail: `.superpowers/sdd/t1-plan/progress.md`.
- **2026-08-21 — T1 QA (controller browser pass) — PASS.** Vite dev @ :5180; verified both themes across 1440×900 / 768×1024 / 390×844.
  - Zero console errors (all viewports/themes).
  - **CLS-0:** hero `<h1>` `min-height` 218px, rect height identical (218px) across a theme toggle → no shift.
  - **No-flash + persistence:** reload applies `localStorage['byte-theme']` before paint; `data-theme`/`data-type`/`data-glow` all set on `<html>` (grotesk/mint defaults).
  - **Theme toggle** button flips + persists; ~400ms token crossfade.
  - **Grain:** `position:fixed`, `pointer-events:none`, `z-index:9999`, runtime noise tile; opacity .06 light / .08 dark (exactly at ceilings).
  - No horizontal overflow at 390px. Tokens resolve (light paper `#f4f1ea`/ink `#16151a`; dark paper `#0e0d12`/ink `#f2f0f5`); Space Grotesk active.
  - **Open for owner:** lock derived palette; `--accent` light `#c1502c`=4.18:1 on paper & `::selection` accent/glow ≈3:1 (WCAG AA caveats — see swatch review). Build/lint/format/test all green (per task reports). SDD ledger: `.superpowers/sdd/t1-plan/progress.md`.
- **2026-08-21 — T0 DONE (no code).** Grill complete; SPEC + companions written & committed (`60378a3`, `d53d217`); ticket roadmap written & committed (`dcab4c3`); roadmap approved; execution style = subagent-driven. `gsap-skills` plugin installed to `~/.claude/skills/`.
- **✅ T7 COMPLETE (2026-08-25; `f15f18e..06f689e`, D-15).** Sound + custom cursor shipped and QA-passed; owner T6/T7 play-test feedback (mentioned, non-blocking) folds into T8. Branch stays OPEN (no merge; T8–T10). `.superpowers/sdd/t7-plan/` audit trail kept.
- **✅ T9 COMPLETE (2026-08-27; `97a7d6e..89f740f`, D-17).** a11y announcer + keyboard feed + live reduced-motion + memory + mobile hardening shipped and QA-passed (both themes × 3 viewports, zero console errors). Branch stays OPEN (no merge; T10). `.superpowers/sdd/t9-plan/` audit trail kept.
- **NEXT → T-Audio (when audio files are delivered), or the owner's PR.** T0–T12 and **T-GLB** are all DONE (see the T-GLB entries above; D-22). T12 shipped on its own branch `lc/byte-pet-scroll-bounds-25b3c2` (off the T10 close `4288559`); T-GLB shipped on `lc/t-glb-byte-model-a0af19`, HEAD `1f91c20`. Both are **unmerged — the owner's call**; their animated on-device residuals (T12's hand-off fade/mid-trip feed/occlusion weave/FED counter/reduced-motion; T-GLB's real-OS reduced motion, true 60fps on a real phone, the doubleSided fade and the visor speckle on real hardware, the slow-network pop on a real connection) are recorded in `docs/qa/README.md` §4. `feat/byte-pet-demo` stays **OPEN** for the one remaining owner-deliverable-triggered swap-in, **T-Audio** (recorded audio → `createHowlerFiles()` behind the same `SoundEngine`) — see `docs/TICKETS.md`. The T10 on-device residuals are recorded with run-instructions + code/unit evidence in **`docs/qa/README.md`** (true-60fps 10s-rAF, 50-feed heap, reduced-motion-on-device, migration 2nd-leg, the ~30s loop GIF + hero stills, and a VoiceOver/NVDA spot-check of the manifesto reveals). The on-device checks + deferred minors listed below are all **optional polish, none blocking** — reconnecting Claude-in-Chrome would let the GIF + a device Lighthouse be captured directly:
  1. **`prefers-reduced-motion` on a real device** — Byte now self-detects LIVE (T9) matching every page module; verify hops/dashes→fades, parallax off, migration snap, blink stays, and the live OS-flip re-detect. The live-region announce fires in BOTH modes (it's BEFORE the `reducedActive` split — code+unit-verified; in-pane the announce was proven in full motion). Not OS-emulable in-pane.
  2. **True 60 fps** 10s rAF sample across the full run (entrance → feed×N → retype → sleep/wake → migrate → footer retype → theme-toggle-mid-dash) + a formal **50-feed heap** profile (T9 QA proved leak-free via scene mesh-count **6→6** + front-children→1 driven by `stepGsap`; a real-ticker fps/heap capture is on-device).
  3. **Migration "feel"** — the 2nd-leg lane traverse (footer→hero re-home) was in fact broken and is fixed and verified in headless Chrome (2026-09-24 entry above); only the on-device "feel" eyeball remains.
  4. **T9 deferred minors (none must-fix — `.superpowers/sdd/t9-plan/progress.md`):** `a11y.test.ts` executor smoke arms a real 1s `setTimeout` (benign; the new region-lifecycle `beforeEach` now clears it); `main.ts` no-WebGL button-hide uses `document.querySelector` vs the WebGL path's `root.querySelector` (same element); the keyboard-feed spawn-coord headline fallback is near-dead (revealed button always ≥44px); a doc-comment cites `createBytePet.ts:787-794` vs actual `788-795`; **`.footer__email` underline now sits ~9.6px below the text** (symmetric block padding — owner visual eyeball; an `::after` hit-extender was rejected as it risks overlapping `.footer__counter`).
  5. **T8 deferred minors still open (T9 didn't touch those files):** body-color lerp `from`=theme-base (1-frame snap on rapid double-toggle); sleep+toggle glow brightness; burst-while-traveling extra-glyph timing; `.lab__button` `--paper` vs `--paper-2` + no `:focus-visible`; `buildPanel` 4 positional args. Full list `.superpowers/sdd/t8-plan/progress.md`.
  6. **P-2 halo sprite (still deferred):** SPEC §11 "Glow emissive + halo sprite" — only the emissive accent + `--glow` shipped; add a dark-mode halo tinted by `currentGlowAccent` in scene/rig if the owner wants it (T10/polish).
  7. **Owner T6/T7 play-test feedback** (still pending, non-blocking) folds into T10/polish if it arrives.

## Open owner deliverables (not blocking)
- Byte's rigged `.glb` per `ASSET_SPEC.md` → triggered **T-GLB**, now shipped (D-22).
- Recorded sounds per `AUDIO_SPEC.md` → triggers **T-Audio** (late).
- Real email + social URLs (placeholders stand in until then).
