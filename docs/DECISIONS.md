# DECISIONS — technical choices + why

Running log (append per ticket). Newest first. See [`SPEC.md`](SPEC.md) for the full locked spec.

---

## D-17 · T9 hardening: pure-throttle announcer (pre-created polite region), reveal-on-focus keyboard feed via a unified wake-then-feed, live reduced-motion self-detect, per-tick Color hoist, ≥44px touch targets — 2026-08-27
**Choices:** (1) **The phrase announcer keeps the pure-core/executor split** (SPEC §4.3): `pet/a11y.ts` `createPhraseThrottle` is a PURE leading + trailing-coalesce throttle (time via injected `nowMs`; zero imports/DOM/`Date.now`/`Math.random`), and `announcePhrase` is the DOM executor (a lazily-built, visually-hidden **polite `aria-live` + `aria-atomic`** region). It is wired at the SINGLE `handleEnterRetyping` seam — after `const next` and BEFORE the `reducedActive` branch — so a reward retype announces the phrase verbatim (§10) for hero AND footer, full AND reduced, while the **entrance never announces** (it types through `entering`, which never routes through `handleEnterRetyping`; the static headline #1 is already in the a11y tree on load). **The region is pre-created EMPTY at `createBytePet` construction (`ensureAnnouncerRegion`) and torn down in `destroy()` (`teardownAnnouncer`)** — the whole-branch review's one Important finding: injecting AND populating a live region in the same tick drops the FIRST announcement on some screen readers, so the empty region must be registered ≫1s before the first feed. (2) **Keyboard feed = a reveal-on-focus real `<button>` + a unified wake-then-feed `feed()`** (SPEC §12): the affordance is a PAGE concern (`index.html` `[data-feed-byte]` + `global.css` `.hero__feed-key` skip-link reveal at ≥44px `position:fixed` with an `--accent` `:focus-visible` ring; `main.ts` wires the click; `[hidden]` on the no-WebGL floor) so `pet/` stays portable, while `feed()` now sends `POINTER_DOWN` (wake) THEN `feeder.feed()`, with `onPointerDown` collapsed to call `feed()` — ONE entry for pointer + keyboard + AT-activation, pointer byte-identical, no double-send. The wake-BEFORE-feed order is load-bearing (the FSM ignores `FEED` while sleeping — `fsm.test.ts:646`). Chosen over a focusable hero region (owner-approved: cleaner SR semantics — "Feed Byte, button" — and it preserves the minimalist hero). (3) **Reduced motion reconciles to LIVE self-detection** (realizing R-T4-7): `main.ts` stops passing `reducedMotion` into `createBytePet`, so the module's own `gsap.matchMedia()` else-branch drives `reducedActive` LIVE — matching every page module (`hero`/`manifesto`/`reveals`/`grain`), which already live-detect; main's own boot capture still keys the one-time `willByteType`/`runEntrance` entrance branch. Safe by construction: `scene.ts` never reads `reducedMotion` (grep-confirmed), `createFeeder` reads the live `reducedActive` closure. This is NOT a new detector — it hands Byte to the matchMedia wiring already built. (4) **Memory = hoist, not re-architect:** the 2× per-frame `new THREE.Color` in `applyTheme`'s lerp `onUpdate` (+ `setDimmed`) become closure-scoped reused scratch Colors (`themeLerpColor.copy(from).lerp(to,p)`), safe because `rig.setBodyColor` COPIES its argument (`source.body.color.set()`, rig.ts:470 — guarded by 2 new `rig.test.ts` tests). The 50-feed heap stability actually rests on the pre-existing feeder disposal discipline (glyph geo/mat freed per eat, one shared particle geometry, `track()`-pruned tweens) — QA-confirmed leak-free (scene mesh/points **6→6**); remaining `onTick` transient allocations (`worldFromScreen` literals, `Range`/`DOMRect`) are short-lived GC'd sawtooth, not leaks — documented, not edited. (5) **Mobile = pad the hit area, not the icon:** `min-inline/block-size:44px` on the nav toggles (glyph + EQ bars untouched) and `padding-block` on the flex-child `.footer__email` reach ≥44×44; no-pills-on-touch (T7's `(hover:hover) and (pointer:fine)` gate) + no-overflow @390px re-verified. Canvases were ALREADY `aria-hidden` + `pointer-events:none` + non-focusable (scene.ts:262) — verified, no change.
**Why:** the pure-core/executor + single-seam wiring keep the announcer unit-tested and framework-agnostic while structurally guaranteeing both-homes/both-modes coverage AND entrance non-coverage; pre-creating the region is the standard remedy for SR first-announce loss; unifying `feed()` gives pointer/keyboard/AT one tested wake-then-feed path; live self-detection makes Byte consistent with every other motion system on the page; and the memory + mobile passes harden without adding rendering or runtime deps. **No new runtime deps.** JS **209.90KB gz** / CSS **3.41KB gz** / **243 tests** (+14 vs T8's 229). Executed via `subagent-driven-development`: 5 tasks + per-task Opus reviews (Task 2 recovered from a mid-response machine-sleep crash by ADOPTING the intact 4-file working tree + verifying it vs the brief + committing — not resetting) + **Opus whole-branch review** (Ready-with-1-fix) + 1 fix wave (`89f740f`) + scoped re-review (all addressed) + **controller browser QA — PASS** (both themes × 1440/768/390: canvases-aria, first-feed announce, keyboard feed, `feed()`-wakes, 50-feed heap, touch targets, zero console errors; reduced-motion-live + true-60fps deferred on-device to T10, code+unit backed). Rulings **R9-1** announcer at the single `handleEnterRetyping` seam (entrance excluded) · **R9-2** reveal-on-focus button + unified wake-then-feed (owner-approved over a focusable region) · **R9-3** reduced-motion LIVE self-detect (drop the opt, not a new detector) · **R9-4** Task-2 crash recovery = adopt-the-working-tree · **R9-5** footer-underline offset + the other T9 minors deferred to T10 / owner eyeball; **P-2 halo sprite stays deferred**. **T9→DONE; NO merge (branch open for T10); `.superpowers/sdd/t9-plan/` audit trail kept.**

---

## D-16 · T8 migration + theme reaction + Style-Lab: reversible scroll lane, single-engine active-home model, rig-owned material lerp + Byte stretch, lab axes wired via CSS-token + MutationObserver — 2026-08-26
**Choices:** (1) **Migration is a per-tick, LIVE-`pickAnchor` decision, not a captured destination** (R8-2): `runMigrationFull` recomputes `pickAnchor(heroRect, footerRect, {height})` every tick and drives BOTH the right-margin lane point AND the arrival check off that live pick, so a mid-trip scroll reversal turns Byte around instead of stranding it. `MIGRATE` fires once per band-crossing (a `wasActiveInBand` edge guard mirroring `wasNear`/`wasHome`); arrival re-homes through the EXISTING `setHomeAnchor` + `wasHome` no-snap reacquire seam (no new home-handoff code), and explicitly `killTweensOf(position)` + kills the reacquire glide so the lane `quickTo` and the reacquire never co-write the root (single-writer). Reduced motion (SPEC §12) → an instant snap, but ONLY to an in-band pick (final-review finding #2: an unconditional snap parked Byte at an off-screen mid-page anchor and churned MIGRATE/ARRIVED — now the `pickInBand` gate leaves the `wasActiveInBand` edge armed and holds until a real target appears). (2) **The retype engine stays SINGULAR across two homes** (carry-forward #1, D-13/D-14): `createBytePet` gained a switchable "active home" (`setHomeAnchor` swaps the anchor el + its own caret + `data-byte-line` spans + phrase cycle + `phraseIndex`), and the FOOTER retype just re-points that ONE `createRetype`/`onTick` driver at the footer element + `phrases.footer` — a second typing/caret path would be a defect. Only the ACTIVE home shows its blinking caret (R8-1 — SPEC §6 "one caret at Byte's home"); the inactive home's caret is hidden via `visibility` (CLS-safe, since `opacity` is blink-animated), toggled by `setHomeAnchor`. (3) **Theme reaction keeps D-11's split:** `scene.ts` lerps LIGHTS only (`setTheme(t, durationS)` GSAP-tweens hemisphere/key color+intensity, never materials — R-T4-5; kill-before-restart), while `createBytePet.applyTheme(t, {animate})` lerps Byte's rig-owned Body/Glow (~400ms via a `{p:0→1}` proxy → `rig.setBodyColor` + emissive, keeping the rig interface unchanged) + a full-height `scale.y` squash-stretch (~1.18×`unitPx` on the ROOT, so it can't fight a clip) + the dark phosphor glow. T7's `themeWhoosh` stays in the public `setTheme` wrapper (fires once per toggle, never at construction); construction + reduced motion take the instant path. **`src/lib/theme.ts` was NOT modified** (P-1): the ~400ms token crossfade already lives in CSS (T1) and the nav toggle already calls `bytePet.setTheme` via `main.ts`, so the 3D lerp belongs in scene/rig — the ticket's stale `theme.ts` file-hint was skipped. (4) **The glow axis drives Byte's emissive accent + the `--glow` CSS token, NOT a halo sprite** (P-2): SPEC §11 names "Glow emissive + halo sprite" but no halo sprite was ever built (only the blob shadow shipped in T3/T4); the emissive accent is the screenshot-verifiable behavior, and a dark-mode halo tinted by `currentGlowAccent` is deferrable to T9/polish. `currentGlowAccent` is per-instance state (final-review minor #3). (5) **The Style Lab owns only the attribute flip; `main.ts` is the page↔pet bridge** (R7-8 extended): `lab.ts` adds `data-glow`(mint|amber|white|cyan) + `data-phrase-set`(identity|punchy|combined) radio axes + a "Copy locked config" button (reads the 4 axes → `{type,glow,phraseSet,tooltip}`, `console.log` + best-effort guarded `navigator.clipboard`); `main.ts` watches those attributes with a `MutationObserver` (inside the WebGL `bytePet` block) and calls `bytePet.setGlowAccent(getComputedStyle('--glow'))` — reading the CSS token keeps `pet/` portable + in lockstep with `tokens.css` — and `bytePet.setPhrases(identity | punchy | [...identity, ...punchy])`. `setPhrases`'s exact next-phrase index is dev-lab latitude (P-3). **No new runtime deps** (gsap/three/lenis/WebAudio all already in the bundle); `fsm.ts`/`retype.ts` + the new pure `anchor.ts` import neither gsap nor three (no wall-clock/`Math.random`). JS **209.41KB gz** / CSS **3.29KB gz** / **229 tests**. Rulings: **P-1** skip `theme.ts`; **P-2** glow = emissive accent + `--glow`, not a halo sprite; **P-3** `setPhrases` next-index latitude; **R8-1** one caret at the active home; **R8-2** reversible migration off the live pick; **R8-3** surgical "T5 task 5"→"T8 task 5" comment fold (6 spots; the 22 legit ticket-T5 / `R-T5-*` refs preserved); **R8-4** the T8 whole-branch review was scoped to the ticket's `6c92333..HEAD` (10 commits), NOT the plan's literal `merge-base main HEAD` (`cd96f35..HEAD` = the whole 70-commit project) since `feat/byte-pet-demo` is a long-lived multi-ticket branch whose T1–T7 are each already reviewed + QA'd.

---

## D-15 · T7 sound + custom cursor: swappable `SoundEngine` (synth) behind a first-gesture gate, engine-agnostic cue wiring, custom cursor pills, lab tooltip axis — 2026-08-25
**Choices:** (1) **Sound is a thin swappable `SoundEngine` interface** (`pet/sound/SoundEngine.ts`: `unlock/setEnabled/enabled/play(cue)/setMaster`; `Cue` = the 7 named cues; + an exported `silentSoundEngine` null-object) with a **WebAudio-synth** impl now (`webAudioSynth.ts` `createWebAudioSynth()` — all cues code-generated, ZERO assets) and **Howler + recorded files as the LATER T-Audio swap** (D-07; `AUDIO_SPEC.md`). Every cue fires through `sound.play(cue)`; the ONLY WebAudio reference in the tree is inside `webAudioSynth.ts`, and `main.ts` is the SOLE construction site — so the Howler swap changes one import + one line and touches NO call site. (2) **The engine is INJECTED, not a boolean** (R7-1): a new additive `PetOptions.sound?: SoundEngine`, defaulted to `silentSoundEngine`, threaded into `createBytePet` + the feeder; `main.ts` constructs ONE `createWebAudioSynth()` and shares it between the pet cues and the page controls (EQ/gate). This supersedes SPEC §4.4's sketch `sounds?: boolean` — the shared instance is what lets the nav EQ toggle mute the same engine the pet plays through. `fsm.ts`/`retype.ts` stay PURE (cues hook in `createBytePet.ts`/`feed.ts`, which already import gsap/three). (3) **Gate = Léo pattern:** muted until the first user gesture (`unlock()` lazily creates + `resume()`s the AudioContext), ON after (D-05), `enabled` persisted to `localStorage` (`byte:sound`); `play()` no-ops unless `unlocked && enabled`. Unlock fires on the first `pointerdown` OR `keydown` (capture, one-time) so keyboard-only users aren't locked out (SPEC §12/§15). (4) **`typeTick` shares ONE throttle with the caret spark** at the per-edit retype detector (R7-6): every 3rd edit, skipping `total===0` and `reducedActive`, with the driver gate kept `retyping||entering` so Byte's headline ENTRANCE (which live-types phrase #1 through that same driver) is audible — the load-bearing carry-forward. `eatA/eatB` alternate on the two chomps; `spawnPop` on toss; `themeWhoosh` in the `setTheme` wrapper (not construction, which also calls `applyTheme`); `wakeBoing` on wake; `chirp` on peek only (R7-2 — `wakeBoing` owns the wake moment). (5) **Custom cursor** (`lib/cursor.ts` `initCursor()`) is a zone-agnostic dot→pill mechanism; the FEED/TOGGLE/OPEN zone mapping lives in a `main.ts` `pointermove` resolver (R7-4: `a[href]`→OPEN, toggles→TOGGLE, `#hero`→FEED). No pills on touch: `matchMedia('(hover: hover) and (pointer: fine)')` gates the whole cursor off (R7-5) — the idiom, newly authored here (none existed), is reused by hero.ts's gate label. (6) **Style-Lab tooltip axis** is split (R7-8): `lab.ts` only flips `data-tooltip` on/off (mirroring the type axis); the `still hungry` runtime tooltip (shown after eaten≥3 AND `data-tooltip='on'`) lives in `main.ts`, inline-styled + CLS-safe, re-evaluated by a `<html>` MutationObserver. **No new runtime deps** (WebAudio is a browser API; Howler deferred). JS **207.34KB gz** / CSS **3.26KB gz** / **207 tests**.
**Why:** the interface + null-object keep the demo shipping on synth (zero downloads) while making the Howler swap wasteless and call-site-free (mirrors the placeholder→GLB pattern); injecting one shared engine is what unifies the pet's cues with the nav mute; the layered gate (engine `play()` guard + reduced/`total===0` suppression) makes pre-gate audio and per-char-under-reduced structurally impossible; and routing reduced-motion + touch through single matchMedia gates keeps the garnish from ever blocking first paint, breaking CLS-0, or firing on the wrong device. Executed via `subagent-driven-development` (6 tasks; per-task + Opus whole-branch reviews; 2 fix rounds — T1 async `resume()`-rejection handler, T4 keyboard-only unlock); the whole-branch review returned Ready=YES with all 12 deferred minors carried (none must-fix-before-T8). **T7→DONE; NO merge (branch open for T8–T10); `.superpowers/sdd/t7-plan/` audit trail kept.** Rulings R7-1..8.

## D-14 · T6 entrance half: reuse-the-engine live-type from empty, `hidden→entering→idle` beats, no-flash/never-hang preloader — 2026-08-24
**Choices:** (1) **The entrance types phrase #1 by REUSING the retype engine + the single `onTick` driver — no second typing path.** A pure `createRetype().reset(lines)` re-seed (R-T6b-1) lets the entrance type from `['','']`: `createBytePet` seeds the engine unchanged (`initial: cycle[0]`, so a never-entered instance is consistent), and `enterAndType()` calls `reset(['',''])` → `enqueue(cycle[0])`; on completion the engine lands `current=cycle[0]`/`phraseIndex=0`, byte-identical to a non-entrance construct, so the retype-reward loop is provably unregressed. The `onTick` retype block's gate is generalized from `retyping` to `retyping||entering` (R-T6b-3), firing `ENTERED` vs `RETYPED` by state; the caret-rest write is inverse-gated. ONE typing path, ONE caret path (`renderRetype`). (2) **`hidden`/`entering` made reachable via `createFSM({ initialState })` + new `SHOWN`(hidden→entering)/`ENTERED`(entering→idle) events + an `enteringMs`(8000) safety cap** (R-T6b-2 — the exact REACHED/ATE/RETYPED mirror; the engine sends the beat, the pure FSM accepts it). `initialState` defaults `'idle'` (read off cfg, kept out of `DEFAULTS`), so every existing FSM test + the non-entrance path is unchanged; `fsm.ts`/`retype.ts` stay PURE (no gsap/three/wall-clock/`Math.random`). (3) **`createBytePet.enterAndType(): Promise<void>`** (R-T6b-4) owns the Byte-side choreography: hidden-start construction hides Byte+shadow and (full motion only) clears the headline to empty behind the overlay (no phrase-#1 flash); full path = reveal → `SHOWN` → `await` a `bounce.out` drop-in (scaling `pose.scale`, identity 1 — NOT the `unitPx`-scaled root) → `reset`+`enqueue`+`retypeActive` (the awaited drop-in completing BEFORE `retypeActive=true` keeps the drop-in tween and the `byteToCaret` `quickTo` off the same frame; `entering ∉ HOME_STATES` so no anchor-pin fight) → the reused driver types + follows the caret → `ENTERED`; resolves via a one-shot `entering→idle` detector. **`page/preloader.ts` `runEntrance(deps)`** owns the page side (overlay %, gate, lift, `await bytePet?.enterAndType()`, chrome stagger), keeping `pet/` page-agnostic. (4) **The preloader is no-flash AND never-hang AND JS-off-safe together** (R-T6b-5): the overlay is STATIC markup in `index.html` (covers from first paint, no FOUC), hidden JS-off by `<noscript><style>`, force-cleared by a dependency-free inline `<head>` 6s fallback if the module never boots, and lifted by `runEntrance` after a gate that races `document.fonts.ready` against a bounded timeout AND a MIN floor (resolves in `[900,4000]ms`, never longer). (5) **The hero SplitText line-reveal is suppressed ONLY when Byte live-types the headline** (R-T6b-6): `initHero({ headlineReveal: !willByteType })`, always keeping `ScrollTrigger.refresh()`. (6) **R-T6b-9 (ruled mid-execution): `main` passes `entrance: webgl` (NOT `willByteType`)** — "Byte participates in the entrance" is true whenever WebGL exists, and `enterAndType()` branches full vs reduced INTERNALLY via `reducedActive`; `willByteType = webgl && !reducedMotion` keys ONLY the reveal-suppression. So `hidden→entering→idle` runs in BOTH motion modes (reduced places Byte at the anchor instantly, keeps static phrase #1) and Task 2's reduced branch is live, not dead. The entrance is `void runEntrance(...).catch(...)`-driven on both the WebGL and no-WebGL paths (first-paint-unblocked, rejection-safe). **No new runtime deps.** JS **205.35KB gz** / 176 tests.
**Why:** the reset-and-reuse keeps ONE unit-tested pure schedule + ONE render path driving both the entrance and every reward retype (the SPEC §8.1 "reuses the retype engine" mandate) while GSAP animates Byte and the DOM; the event-driven `hidden`/`entering` keeps the FSM a pure function of config+events+ms; the layered gate + FSM caps + `<noscript>` + inline fallback make the preloader structurally unable to hang or leave the page covered; and R-T6b-9 routes reduced motion THROUGH `createBytePet`'s `reducedActive` (per SPEC §12) to the same instant phrase-#1 idle state. **T7 note:** the typing `typeTick` sound MUST hook the generalized `retyping||entering` edit-detector (per actual edit) or the entrance types silently; the entrance re-arms `lastRetypeTotal=-1` so the first driven frame emits one deliberate spark on the empty headline (expect/suppress); reduced motion emits no per-char events. This half flips **TICKETS T6→DONE** (both halves complete) but deliberately does NOT merge (owner play-test gate; branch open for T7–T10) and keeps the `.superpowers/sdd/t6b-plan/` audit trail. Rulings R-T6b-1..9.

## D-13 · T6 retype-reward half: pure span-engine, `ATE`→`retyping` reroute, live-caret DOM contract — 2026-08-24
**Choices:** (1) **The retype schedule is a PURE decision-core** (`createRetype`/`buildRetypeSchedule` in `retype.ts` — imports neither gsap nor three, no wall-clock, no `Math.random`; jitter/cadence come only from an injected `random: () => number`, default `() => 0.5`), unit-tested by `retype.test.ts` (24 tests), mirroring the SPEC §4.3 decide/execute split and the `fsm.ts`/`glyphs.ts` precedent. `renderRetype` (same file, DOM-only) applies a frame to per-char `.byte-char` spans + moves an absolutely-positioned caret via `transform`. This realizes **D-03** (a custom span engine, NOT SplitText — SplitText stays only for the manifesto/hero masked reveals). (2) **`retyping` is made reachable by rerouting `eating --ATE--> retyping`** (was `→idle`) + a new retype-driver-fired **`RETYPED`** event (`retyping→idle`) + a generous `retypeMs`(4000) safety cap — the exact REACHED/ATE mirror (engine sends the beat, the pure FSM accepts it). The linear `dashing→eating→retyping→idle` chain serializes each feed's eat→retype→queue-drain (the feeder drains on `onEnter('idle')`), avoiding a race with an off-`onEat` trigger; the `eating` safety cap still exits to `idle` (emergency path skips the reward — deliberate asymmetry). (3) **`createBytePet` owns the retype DOM but stays page-agnostic:** it tags the two headline line elements with its OWN `data-byte-line="0|1"` attributes and creates a `data-byte-caret` caret (never a page CSS class), so `renderRetype` resolves them by attribute — robust to the hero's SplitText wrapping. (4) **The caret is resolved LIVE at every use** (`headlineEl.querySelector('[data-byte-caret]')`, re-appending a seed if absent) and positioned via `getBoundingClientRect` deltas relative to `#hero-headline` — because the hero load-reveal SplitText's revert restores CLONED headline children, which would otherwise orphan a captured node reference (caught in browser QA; `offsetParent`-relative offsets were likewise fragile). (5) **Reduced motion** → an instant text set via `enqueue; step(0); step(MAX)` (a lone `step(MAX)` anchors the engine clock and renders the STALE phrase — R-T6a-9) with no Byte glide/spark; the caret's CSS blink stays (SPEC §12). (6) **Phrase cycling** is wired via a new additive `PetOptions.phrases` (`main.ts` passes `phrases.identity`); the lab phrase-set axis (Identity/Punchy/combined) remains T8. **No new runtime deps** — gsap/three were already in.
**Why:** the pure-core/executor split keeps the retype decision logic unit-tested + framework-agnostic while GSAP animates Byte and the DOM; the event-driven `retyping` keeps the FSM a pure function of config+events+ms even as per-char timing varies; the attribute-based, live-resolved caret makes the retype robust to SplitText's DOM churn without coupling `pet/` to page markup. **Entrance-half note:** `hidden`/`entering` become reachable via the SAME engine-beat pattern, the entrance's live-type reuses `createRetype`/`renderRetype` from an empty start, and the audible "soft tick every 2–3 chars" (`typeTick`) is deferred to T7 (no `SoundEngine` until then). This half did NOT flip TICKETS T6→DONE (entrance pending), did NOT merge, and kept the `.superpowers/sdd/t6a-plan/` audit trail.

## D-12 · T5 feeding loop: pure glyph-queue core, event-driven `eating` (REACHED/ATE), sleep-allowlist fix, footer counter — 2026-08-23
**Choices:** (1) **The glyph queue/counter is a PURE decision-core** (`createGlyphQueue` in `glyphs.ts` — no gsap/three) that `createFeeder` (`feed.ts`) executes with GSAP/three, mirroring the SPEC §4.3 decide/execute split and the FSM precedent; `glyphs.test.ts` unit-tests it (cap 3 pops the oldest non-eating entry, `onEat` increments the running total) plus `makeGlyph` (6 bevel-extruded `THREE.Shape` kinds `{ ; > * + =`, no `typeface.json`) — the ticket's tested core, needing no `WebGLRenderer`. (2) **`eating` is made reachable via explicit feeder-fired FSM events `REACHED` (dashing→eating) and `ATE` (eating→idle)**, NOT by matching a fixed FSM timer to a GSAP duration (the dash is 380–600ms *by distance*); the durations live in the animation layer and the FSM stays pure/deterministic. Generous safety caps (`dashMs`→1200, new `eatMs`=1500) exit to `idle` only if a beat is ever dropped, so the FSM can't wedge. (3) **MUST-FIX (carry-forward #1): `isSleepEligible` denylist → explicit awake-resting-state allowlist `{idle, curious, invited, peeking}`**, landed in the SAME change that makes `eating` reachable — it excludes `dashing`/`eating` (Byte finishes feeding before the 30s-idle overlay) and the unreachable/active `hidden`/`entering`/`retyping`/`traveling`, and keeps `peeking` (peeks deliberately don't reset the sleep accumulator, so a late peek must still be able to sleep out). (4) **The feeder drives dashing/eating choreography via `fsm.onEnter`** (enter dashing → dash to the front-of-queue glyph → `REACHED`; enter eating → chomp into `mouthWorld()` + particles + count + dispose → `ATE`; enter idle → drain the queue via a legal `FEED`), so one path covers both the idle/curious/invited→`FEED`→dashing route and the sleeping→`POINTER_DOWN`→waking→dashing "wake counts as a feed" payoff; `createBytePet.dispatch()` therefore no longer plays `Dash`/`Eat` (no double-drive) and gained a **stale-transition guard** (`if (fsm.state() !== state) return;`) after a nested re-entrant transition (the feeder re-chaining idle→`FEED`→dashing synchronously inside `enter('idle')`) was found replaying `Idle` over the fresh Dash and fighting the bank. (5) **Root-placement handoff:** `onTick` hard-pins Byte to the headline anchor only in home states; the feeder owns `rig.object3d.position` during dashing/eating; a no-snap "reacquire" glide returns Byte to the anchor on the first home tick; the blob-shadow follows the actual root position, not the recomputed anchor (its `MAX_HOVER_HEIGHT=64` vertical tuning is unchanged — the dash is horizontal locomotion, only the tossed glyph arcs vertically). (6) **The FED counter lives in the FOOTER** (`[data-fed-counter]`, SPEC §8.6 — TICKETS' "hero.ts" file-hint was stale; the element shipped in the footer at T2); `createBytePet` surfaces the running total via `BytePetHandle.onEat`, and `main.ts` renders `FED N GLYPHS`, keeping `pet/` free of page copy. (7) **`createFeeder`'s signature is extended** with an additive `deps` bag `{ prefersReducedMotion: () => boolean; unitPx: number }` (reduced-motion honored per-feed via a live accessor reading `createBytePet`'s `reducedActive`; glyphs sized to the headline scale). **No new runtime deps** — `THREE.Shape`/`ExtrudeGeometry`/`Points` and GSAP were already in the bundle.
**Why:** the pure-core/executor split keeps the feeding decision logic unit-tested and framework-agnostic while GSAP/three animate; event-driven `eating` keeps the FSM a pure function of config + events + ticked ms even as tween durations vary by distance and motion preference; the allowlist fix is the carried-forward correctness requirement (no sleeping mid-eat) applied exactly when `eating` becomes reachable; the onEnter-driven feeder plus the stale-transition guard make the choreography robust to the synchronous re-entrancy the queue-drain introduces; the root-placement handoff resolves the dash-locomotion-vs-anchor-pin conflict without a visible snap; and the footer counter honors SPEC §8.6 while keeping the module portable. **T-GLB note:** the real GLB rig must still expose `mouthWorld()` (glyphs converge there) and in-place `Dash`/`Eat` clips (the feeder owns locomotion + the glyph tween).

## D-11 · T4 rig-owned Body/Glow theming + scene layer helpers + pure config-injected FSM — 2026-08-23
**Choices:** (1) **Byte owns its material theming via the `PetRig`** (`setBodyColor`/`setGlow`); `scene.setTheme` now re-lerps **lights only** — the T3 blanket "repaint every `MeshStandardMaterial`" was removed (its sole consumer was the deleted `?glcube` cube). On a theme change `createBytePet` calls `scene.setTheme(t)` + `rig.setBodyColor(bodyColorForTheme(t))` + `rig.setGlow(t==='dark', DEFAULT_GLOW_ACCENT)` (mint `#38e8a8`; the lab glow axis lands at T8). **T-GLB's real rig must own `Body`/`Glow` the same way** (glow ≠ body recolor — SPEC §11 phosphor accent). (2) **`scene.addToPet(obj)` / `addToFront(obj)`** helpers retire the T3 layer footgun (three.js render layers don't cascade from a parent `Group`): `addToPet` re-applies the current behind/front layer to the object + descendants; `addToFront` sets `FRONT_RENDER_LAYER` on them. **T5's glyphs/particles use `addToFront`.** (3) **The FSM (`fsm.ts`) is pure + deterministic + config-injected** — imports neither gsap nor three, no wall-clock/`Math.random`; time enters only via `tickTimers(dtMs)` (milliseconds). All randomness + micro-behaviour scheduling live in `createBytePet`, not the FSM. `tickTimers` performs at most one transition per call, evaluating the current state's own timed exit **before** the generic idle→sleeping overlay (a peek/curious completes before Byte sleeps). (4) **`createBytePet` owns the scene** (SPEC §4.4) — it calls `createScene`/`startTicker` internally; `main.ts` only holds the returned `BytePetHandle`. **No new runtime deps:** `RoundedBoxGeometry` ships inside the installed `three@0.185.1` (`three/addons/geometries/RoundedBoxGeometry.js`).
**Why:** the blanket material repaint would clobber the emissive glow with the body color; rig-ownership keeps glow independent and mirrors the placeholder→GLB swap contract. The layer helpers prevent a recurring invisible-mesh bug as T5+ add `frontLayer`/`petLayer` call sites. FSM purity keeps the decision core unit-tested (39 tests) and framework-agnostic while GSAP/`AnimationMixer` execute. Scene ownership matches the locked module API.

## D-10 · T3 runtime dep: three@^0.185.1 (+ @types/three devDep) — 2026-08-22
**Choice:** Added `three@^0.185.1` under `dependencies` — the last big locked-stack dependency (SPEC §14). Installed `three` doesn't bundle its own `.d.ts` files (checked `node_modules/three/package.json`: no `types`/`typings` field, and zero `*.d.ts` files under `node_modules/three`), so `@types/three@^0.185.4` was added under `devDependencies` so `tsc` can resolve `three`/`three/addons/*` types. Confirmed via context7 (`/mrdoob/three.js`) that the addon import paths for this version are `three/addons/loaders/GLTFLoader.js` and `three/addons/loaders/DRACOLoader.js` (both resolve under `examples/jsm/loaders/` per the package's `exports` map, `"./addons/*": "./examples/jsm/*"`) — recorded now for Task 4/T-GLB, not used in this task. DRACO decoder path is `/draco/` (`DRACOLoader.setDecoderPath('/draco/')`, decoder files served from `public/draco/`); no decoder files ship yet — nothing decodes before T-GLB. `RoundedBoxGeometry` (placeholder body) is deferred to T4.
**Why:** three earns its keep starting T3 (WebGL occlusion sandwich); locking the version now lets DECISIONS and the Task 4 addon-loader work point at the same release. Bundle math: three ≈ 165KB gz is the bulk of the 280KB JS ceiling (SPEC §14) and T2 measured 54.8KB gz, so there's headroom even before code-splitting or tree-shaking; the actual post-three number is measured and logged in Task 5 QA, not guessed here.

## D-09 · T2 runtime deps: gsap 3.15 (+ ScrollTrigger, SplitText) + lenis — 2026-08-22
**Choice:** Added the first runtime deps beyond tooling — `gsap@^3.15.0` (core + `ScrollTrigger` + `SplitText`) and `lenis@^1.3.0` — under `dependencies`. SplitText is used only for the manifesto/hero masked line reveals (per D-03, the headline retype stays a custom span engine, not SplitText). Lenis drives smooth scroll, wired to ScrollTrigger via `lenis.on('scroll', ScrollTrigger.update)` and driven off `gsap.ticker` (one clock).
**Why:** All three are in the locked stack (SPEC §14) and first earn their keep in T2 (scroll + reveals). SplitText is free on npm since gsap 3.13, so no Club/paid dependency. Recording per the "no new runtime deps without a DECISIONS entry" rule; installing lazily at first-use (not T1) kept earlier tickets lean (D-08 §5). Reduced-motion + JS-off paths do not depend on any of them.

## D-08 · T1 scaffold, tokens & fonts — 2026-08-22
**Choices:** (1) Removed the inherited Next.js portfolio; the worktree root is the Vite + vanilla-TS app (SPEC §3/§14). (2) Tokens as CSS custom properties, combined `:root, :root[data-theme='light']` (etc.) selectors; axes `data-theme` (light/dark), `data-type` (grotesk/mono/clash), `data-glow` (mint/amber/white/cyan). **Palette locked (owner):** light paper `#f4f1ea`/ink `#16151a`; dark paper `#0e0d12`/ink `#f2f0f5`; accent `#a8451f` light (AA-clean) / `#ff8a5c` dark; glow default `mint #38e8a8`. (3) Fonts self-hosted Latin-subset woff2 — Space Grotesk + JetBrains Mono (Fontsource), Clash Display (Fontshare); `@font-face` at top of `tokens.css`; ~99KB, per-glyph subsetting deferred to T10. (4) `initTheme()`: system pref default, `localStorage['byte-theme']` override, no-flash inline script sets all 3 axes pre-paint; storage access guarded (degrades to system pref, never throws). (5) Node via mise + `.tool-versions` (node 22); T1 installs tooling deps only (three/gsap/lenis deferred to the tickets that first use them).
**Why:** Matches SPEC's Vite/token/lab architecture; keeps T1 unblocked and in-budget; guarded storage + no-flash + CLS-0 groundwork protect the graceful-degradation constraints from the start.

## D-07 · Sound: `SoundEngine` interface, synth now + Howler/files later — 2026-08-21
**Choice:** WebAudio synth behind a thin swappable `SoundEngine` interface; Howler + recorded audio files are a later drop-in swap (see `AUDIO_SPEC.md`).
**Why:** Howler only earns its keep with recorded audio *files*; synth needs no library. The interface means adding Howler is never wasted and switching touches no call sites — mirrors the placeholder→GLB pattern. Unblocked now (zero downloads), asset-rich later. Brief §5 explicitly sanctioned Howler "if we choose assets."

## D-06 · Uses installed `gsap-skills` plugin — 2026-08-21
**Choice:** Installed GreenSock's official `gsap-skills` (8 skills) to `~/.claude/skills/`; use for all GSAP work; fold its `CLAUDE.md` conventions into the project at scaffold.
**Why:** Owner wants to learn GSAP; official guidance keeps usage idiomatic (timelines, `quickTo`, ScrollTrigger, cleanup).

## D-05 · Sound gate ON after first click — 2026-08-21
**Choice:** Muted until first click (Léo pattern); ON after the gate. Byte also gets an occasional soft chirp.
**Why:** Autoplay policy compliance + owner preference.

## D-04 · Two `WebGLRenderer`s (front/back) sharing one scene+camera — 2026-08-21
**Choice:** The two-canvas occlusion sandwich uses two renderers; Byte draws to front or back per a `behind` flag (food+shadow always front).
**Why:** Only way to truly weave a WebGL character behind/in front of crisp DOM type. 2 GL contexts is a non-issue; the scene is tiny so the double render is cheap.

## D-03 · Custom span-based retype engine (not SplitText) — 2026-08-21
**Choice:** Headline backspace/type is a custom, pure, unit-tested engine over per-char spans. SplitText is used only for the manifesto masked reveals.
**Why:** The retype needs per-char control synced to the caret + a testable queue; SplitText is the wrong tool for that.

## D-02 · Byte = user-supplied GLB robot; placeholder now, swap later — 2026-08-21
**Choice:** Character is a little robot from a rigged `.glb` the owner provides (baked clips). Built now against a procedural placeholder + `PetRig` adapter; real model swapped in via GLTFLoader+DRACO later (see `ASSET_SPEC.md`).
**Why:** Owner wants a real modeled creature, not a caret; I can't produce a rigged GLB. Placeholder keeps the whole build unblocked and the real model drops into the same interface. (Deviates from brief's procedural caret — see SPEC §2 D1–D3, D6–D7.)

## D-01 · Full GSAP for all pet motion — 2026-08-21
**Choice:** All Byte motion runs through GSAP (timelines, `CustomEase`/`back`/`elastic`, `quickTo`) with rendering driven off `gsap.ticker`; `AnimationMixer` plays baked clips. FSM + retype-queue *logic* stay pure and GSAP-free for unit tests.
**Why:** Owner wants to learn GSAP; GSAP is already in the bundle for scroll; `gsap.ticker` gives one clock with the WebGL draw; keeping decision-logic pure preserves testability. Cost accepted: the pet module depends on GSAP at Next.js integration (coexists fine with framer-motion).
